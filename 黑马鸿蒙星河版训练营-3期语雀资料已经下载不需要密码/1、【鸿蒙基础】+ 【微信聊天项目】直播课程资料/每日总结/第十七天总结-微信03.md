手势交互
绑定手势 ：
单个事件
gesture(
longPressGesture()
.onAction(()=>{
})
)
多个事件，组合手势
gesture(
GestureGroup(
mode：3种,
longPressGesture()
.onAction(()=>{
})
PanGesture()
.onActionUpdate(（event）=>{
event.figerList[0].globalX +event.figerList[0].globalY 和 px2vp(display.getDefaultScreen.width)  进行状态判断,是录制还是取消还是转文字
先判断Y区分是否录制，在判断X区分是否取消
})
)
)
权限申请：
1.进应用获取
2.使用时获取
打开新的ability让用户自己开启（跳转应用信息设置页面）
文件操作：fileIo
文件夹
判断有无，没有不能直接创建文件
flag = accessSync(文件夹路径)
mkdir(文件夹路径)  创建文件夹
rmdir(文件夹路径)	删除文件夹
文件
创建 ：fileIo.openSync(文件路径,fileIo.Open.Create)
删除 ：fileIo.unlink(文件路径)
