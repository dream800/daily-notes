


面试题：你都知道哪些修饰符
State
Prop
Link
Provide
Consume
Observed
ObjectLink
LocalStorage
AppStorage
Watch
Require
Builder
BuilderParam
Track
Entry
Conponent
CustomDialog
Preview
Styles
Extend



面试题：鸿蒙里面传值(通信)的方案有哪些？有什么区别？

1.函数组件（结构组件）->自定义构建函数
2.状态组件 ->  comp   
组件->自定义构建函数
传值  (调用函数的时候传递)
传结构 （不存在，自定义构建函数就是最小单元值）
组件 -> 组件
父子组件 ->  单向  Prop  ->  自己可以改，不会同步父组件
  		   双向   Link  ->  不能有默认值，自己改父组件也会同步
场景：如果父组件State修饰的是一个数组对象，但是要把对象传递给子组件，子组件还得修改这个对象，需要用Observed修饰对象的类，子组件ObjectLink接受传递的对象，不能修改这个对象的地址，只能修改属性
跨代组件 -> 双向
提供：Provide
接收：Consume
3.页面之间
LocalStorage
4.UIAbility之间
5.线程之间
   线程与线程间：emitter
   某个线程间：eventHub.emit
   多线程：
TaskPool  :   推荐优先使用
Workder  :   长时间固定耗时
6.进程之间
CommonEventManager
7.应用之间
ability跳转打开新的包，携带want参数
8.设备之间
流转


router.pushUrl  页面跳转
router.bank返回上一页
let want:Want = {
  	devieceId:'',
boudleName:看AppScope
abilityName:module.json5
};
记得带分号
UI页面获取上下文
(getContext() as common.UIAbilityContext).startAbility(want)
