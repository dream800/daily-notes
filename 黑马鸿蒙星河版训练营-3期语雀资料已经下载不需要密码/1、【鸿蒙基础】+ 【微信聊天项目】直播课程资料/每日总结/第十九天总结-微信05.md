- 音频播放：AVPlayer

创建 -> idle  
fdSrc+url-> initialized
onStateChange('state',()=>{
initialized
prepared
playing
complated
seekdown
pause
})
切换播放源需要reset才能继续播放

- 系统相册选择图片

const resultList = await  ImagePicker.select({
max:9
type:photo
})
resultList是所有图片的地址
forEach((result)=>{
1.拷贝到沙箱
打开目标文件  const file = openSync(result,openMode.read_only)
let newFile = getContext().fileDir()+'/xxxx.jpg'
fileIo.copySync(file.fd,newFile)
fileIo.closeSync(file.fd)
2.创建消息

3.发送
})

- 打开系统相机

profile :camerePick.cameraProfile = {
cameraPosition:前置还是后置相机
} 
const result = await cameraPick.pick( getContext(),[
image  , video
] ,profile)

result.resultUri  拍摄图片或视频的地址
result.mediaType:拍摄的类型 imgage ||  video
拷贝沙箱，创建消息，添加列表


