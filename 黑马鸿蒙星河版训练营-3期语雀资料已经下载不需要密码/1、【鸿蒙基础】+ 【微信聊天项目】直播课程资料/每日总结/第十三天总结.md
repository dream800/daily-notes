动画
元素动画
animateTo
animation
@animator
图片帧动画
ImageAnimator()
 .images(数组)
ArrayFrom( Array(数组长度) ，map函数（）=>返回的结果就是数组的每一项 )
转场动画
**元素转场**
**页面转场**
**模态转场（全模态）**
元素转场（写法复杂，不推荐）
隐藏/消失转场
手势
单手势
.gesture(
   TapGesture()
      .onAction(()=>{
触发手势要做的事
})
)

多手势
.gesture(
     GestureGroup(模式：3种，
多个手势....，
     )
)

获取屏幕大小 ： display  或者  onAraeChange
