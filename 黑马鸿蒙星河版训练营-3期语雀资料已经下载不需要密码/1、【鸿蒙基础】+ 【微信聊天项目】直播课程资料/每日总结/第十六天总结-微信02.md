请求接口封装
const req = http.createRequest()
req.request('url')
JSON.parse(req.result as string)  as 数据结构 
需要根据响应结果，封装对应的数据类型，从里往外封装
首选项
context:Context
初始化
记录context为静态属性
获取仓库
	 	const store =	preference.getPreferencesSync(上下文,{ name :'key'})
获取仓库所有的key
store.getAllSync()
获取所有联系人的最后一条数据
defaultUserList.forEach(user=>{
通过用户getStore
获取最后一项:chatList[chatList.length-1]
sort可以排序    sort((a,b)=>{
return a-b  // 升序    b-a降序
})
Object.keys(对象)   可以把对象的所有属性值转成一个数组
})
线程通信
某个线程之间 （UI to UI）  EventHub.emit()  EventHub.on()
线程与线程之间  emitter.emit()  emitter.on()

