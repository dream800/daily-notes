1.搭建静态页面
2.自定义弹窗
blank 自适应大小的容器
点击关闭
onBackPress监听返回键  
counse接受控制关闭的变量
3.点菜的逻辑
点击+
是否首次添加
是，添加菜品对象，数量初始化为1
不是，菜品数量++，列表第一层数据更新（数据第二层更新可以导致检测不到变化，从而没有存到persistenStorage中）
点击-
菜品数量--
到0了吗？
0，移除对应列表的菜品
不是0，菜品数量--

统一存入AppStorage
4.购物车StorageLink绑定数据渲染
5.按钮组件
数量的更新需要依赖一个可以数据变化产生UI更新的数据
this.cartList = StorageLink(cart_list)
封装一个方法，通过cartList得出展示的数量
此时，只要cartList变化了，数量就会更新


