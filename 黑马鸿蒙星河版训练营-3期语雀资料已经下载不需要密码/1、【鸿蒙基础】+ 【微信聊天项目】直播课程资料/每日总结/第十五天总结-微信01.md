
创建项目
项目名称：module.json5     $string:label
项目图标：应用图标      进入图标    module.json5  （不推荐用JSON格式！）
自定义图标  右键resource
搭建基本结构
tabs + tabcontent + tabbar
联系人
过滤后的数组 =   filter(( 数组的每一项)=>{
return   条件（条件成立代表留下这一项，不满足条件的过滤跳）
})
发送消息
chatDetail  ->  sendTextMessage ->  ButtomInput
messageList <-new MessageInfoModel()<-chatDetail<-content<-ButtomInput
聚焦
手动聚焦 ：focusControl.requestFoucs(组件的id)
前提是开启focuable(true)
自动聚焦：defaultFocus(true)
