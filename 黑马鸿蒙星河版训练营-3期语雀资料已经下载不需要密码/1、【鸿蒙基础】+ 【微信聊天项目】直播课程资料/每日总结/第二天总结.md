上节课总结：
线性布局:row + column 
主轴  justifyContent
今天的内容
练习案例-百度搜索
需求：百度案例添加鸿蒙版文字的添加  -> 堆叠布局(Z向盖楼)  ->Stack内，后一个元素永远压在前一个元素上面 ，可以通过translate调整元素自身位置
图片：Image(图片地址)
输入框: TextInput()
按钮：Button('登录')
折行需求 - >  弹性布局Flex({ wrap:FlexWrap.wrap}){}   通过参数设置排列方式(有性能问题，建议用Row和Column代替)
滚动需求 - > 网格布局Grid(){ GridItem(){} }
.columnTamplate('1fr 1fr') //  划分为几列 （不要同时划分行和列，否则不会滚动）
.columnGap(10) //设置列间距
滚动需求：
   scroller:Scroller = new Scroller()
      build(){
   	 Scroll(this.scroller){
// 有且只有一个子组件
//  要想滚动，必须让内容大于Scroll大小
//  默认纵向滚动，如果需要横向，需要手动设置
 }
//  滚动到结束
this.scroller.scrollEdge(Edge.End)
     }
事件绑定：
 给元素添加事件方法 ： onChange(内容改变时)   onSubmit（提交内容时）  onClick（点击时）  onFocus（聚焦） onBlur（失去焦点）
  元素（组件）{
 }.onChange(()=>{
  //  XXXXX做一些事
 提示：1.轻提示（需要引入包，自动关闭）2.弹层提示（需要引入包，点击关闭） 3.弹层提示（不需要引入包，点击关闭）
  PromptAction.showToast({message:''})
  PromptAction.showDialog({message:''})
  AlertDialog.show({message:''})
})
  
需求练习：
1.UI还原
2.点击返回进行轻提示（返回成功）
3.封装成一个组件
![](https://cdn.nlark.com/yuque/0/2024/png/8435673/1709038062012-cce7067f-0809-4944-a8ad-771d46ccd216.png?x-oss-process=image%2Fformat%2Cwebp#averageHue=%23faa70e&from=url&id=qmmMo&originHeight=256&originWidth=406&originalType=binary&ratio=1&rotation=0&showTitle=false&status=done&style=none&title=)


