Navigation  
API9
Navigation (){
1.当前页面的内容
NavRouter(){
2.一个元素用于跳转
3.NavDesitination(){
3.要跳的内容
}
}
}
API11
@Builder
customBuilder( name:string ){
   if(name===''){
条件渲染
}
}
navPathStack:NavPathStack = new NavPathStack()
Navigation（this.navPathStack）{
this.navPathStack.pushPath({
name:'xxxx'
})
}.desination(自定义的builder，也就是路由的出口)

router
pushUrl({
url:''
params:{}
},router.RouterMode.Single)
replaceUrl
back
getLength  最大32
getParams
getState
       注册路由放回阻止事件

    模块跳转
hap  -> ability
hsp  -> 有页面
har  -> 静态资源


1.path
router.pushUrl({
  url:'@bundle:包名/模块名/ets/xxxxx'
})

2.name(了解)


生命周期
页面 多三个
onPageShow
onPageHide
onBackPress
组件(通用)  两个
aboutToAppear
aboutToDisapear

ability
14  - >  8个常用   
ability创建、销毁
windowStage创建、销毁
ability前台、后台
onNewWant ability接收新参数
onBackPressed  ability返回是否销毁

    ability跳转
let want :Want =  {
deviceId
bundleName
moduleName
abilityName
parameters:参数
}
（getContext() as common.ContextUIAbility）
直接打开  startAbility()
需要结果的打开 const result = await  startAbilityForResult


ability返回    terminalSelfWithResult({
resultCode:200,
want:{
deviceId
bundleName
moduleName
abilityName
parameters:参数
}
})

