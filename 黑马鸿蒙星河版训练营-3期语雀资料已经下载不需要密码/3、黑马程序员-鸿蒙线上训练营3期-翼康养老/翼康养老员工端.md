:::success
二次开发模版地址：[https://gitee.com/shuiruohanyu/yikang-elder-care.git](https://gitee.com/shuiruohanyu/yikang-elder-care.git)
完整代码仓库地址：[https://gitee.com/shuiruohanyu/ykyl](https://gitee.com/shuiruohanyu/ykyl)
接口文档地址：[https://apifox.com/apidoc/shared-4a97787e-b0e6-46ec-ae1a-5fc0da5e192e](https://apifox.com/apidoc/shared-4a97787e-b0e6-46ec-ae1a-5fc0da5e192e)
UI设计图地址：[https://codesign.qq.com/s/YDgGjYwLpa0wEVQ](https://codesign.qq.com/s/YDgGjYwLpa0wEVQ)
:::
# 项目介绍
> **    随着全球人口老龄化的加速，养老服务行业面临着越来越多的挑战和机遇。为了满足日益增长的需求，养老机构需要不断提升服务质量，提高工作效率，同时确保老人的生活质量。在这个背景下，员工端应运而生，作为养老系统的重要组成部分，为员工提供了一个全新的工作平台。**


![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1710928097758-2a5ffef7-e38a-4c8e-9292-d44dee422e60.png#averageHue=%23e7c69a&clientId=u2174620b-5394-4&from=paste&height=640&id=u094e89e7&originHeight=1280&originWidth=589&originalType=binary&ratio=2&rotation=0&showTitle=false&size=3021920&status=done&style=none&taskId=ub76509af-5829-4520-b687-e987d3d8b4a&title=&width=294.5)  ![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1710928089052-e04e3bfd-91a3-439f-9a41-d20b8aa7b075.png#averageHue=%23b4ab4d&clientId=u2174620b-5394-4&from=paste&height=640&id=uf78eaae7&originHeight=1280&originWidth=590&originalType=binary&ratio=2&rotation=0&showTitle=false&size=3027040&status=done&style=none&taskId=uab40024e-c9dd-4ff7-8aa6-6ee2151e846&title=&width=295)

- 知识体系-通过项目学到什么？
> 1. **鸿蒙三层架构 hsp 模式**
> 2. **鸿蒙三方常用插件使用**
> 3. **华为手机号授权登录**
> 4. **华为云IoTDA 接入使用**
> 5. **低功耗蓝牙设备通信流程**
> 6. **物联网设备的接入开发**

- 项目总结亮点
> 1. **物联网养老项目-员工使用**
> 2. **提供给养老院护理员使用**
> 3. **核心功能-华为认证登录-护理员1对多管理老人的护理和照看，通过万物互联等物联网设备实现如给老人测量血糖，上传血糖数据，门禁开门/关门，蓝牙/Wifi等HarmonyOS能力集的应用**
> 4. **三层架构-har + 多hsp共享包 + 入口hap包的综合应用**

# 项目创建
> 并不是所有项目都需要用三层
> - 工具类小项目 单层
> - 中小型项目-har + hap
> - 中大型-basic(基础能力提供层)-features(特性层)-product(入口层)

- 三层架构设计概述

[文档中心](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/bpta-layered-architecture-design-0000001725966009-V5?catalogVersion=V5)
> HarmonyOS应用的分层架构主要包括三个层次：产品定制层、基础特性层和公共能力层，为开发者构建了一个清晰、高效、可扩展的设计架构。

> 公共能力-hsp-~~har(静态共享包-会多份拷贝)~~
> 基础特性-hsp-动态共享包- 不论如何打包，永远只有一份
> 产品定制-hap入口-具备ability-具备窗口能力


- **HAR静态共享包**
> **HAR（Harmony Archive）是静态共享包，可以包含代码、C++库、资源和配置文件。通过HAR可以实现多个模块或多个工程共享ArkUI组件、资源等相关代码。**
> 使用场景：
> - 作为二方库，发布到[OHPM](https://ohpm.openharmony.cn/)私仓，供公司内部其他应用使用。
> - 作为三方库，发布到[OHPM](https://ohpm.openharmony.cn/)中心仓，供其他应用使用。
> 
限制：
> - HAR不支持在设备上单独安装/运行，只能作为应用模块的依赖项被引用。
> - HAR不支持在配置文件中声明[UIAbility](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/uiability-overview-0000001774119974-V5)组件与[ExtensionAbility](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/extensionability-overview-0000001820879625-V5)组件。
> - HAR不支持在配置文件中声明[pages](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/module-configuration-file-0000001820879553-V5#ZH-CN_TOPIC_0000001881258481__pages%E6%A0%87%E7%AD%BE)页面，但是可以包含pages页面，并通过[命名路由](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/arkts-routing-0000001885919501-V5#ZH-CN_TOPIC_0000001885919501__%E5%91%BD%E5%90%8D%E8%B7%AF%E7%94%B1)的方式进行跳转。
> - HAR不支持引用AppScope目录中的资源。在编译构建时，AppScope中的内容不会打包到HAR中，因此会导致HAR资源引用失败。
> - HAR可以依赖其他HAR，但不支持循环依赖，也不支持依赖传递。
> 

- **HSP动态共享包**

[文档中心](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/in-app-hsp-0000001774119898-V5)
> **HSP（Harmony Shared Package）是动态共享包，可以包含代码、C++库、资源和配置文件，通过HSP可以实现应用内的代码和资源的共享。HSP不支持独立发布，而是跟随其宿主应用的APP包一起发布，与宿主应用同进程，具有相同的包名和生命周期。**
> 使用场景：
> - 多个HAP/HSP共用的代码和资源放在同一个HSP中，可以提高代码、资源的可重用性和可维护性，同时编译打包时也只保留一份HSP代码和资源，能够有效控制应用包大小。
> - HSP在运行时按需加载，有助于提升应用性能。
> 
限制：
> - HSP不支持在设备上单独安装/运行，需要与依赖该HSP的HAP一起安装/运行。HSP的版本号必须与HAP版本号一致。
> - HSP不支持在配置文件中声明[UIAbility](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/uiability-overview-0000001774119974-V5)组件与[ExtensionAbility](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/extensionability-overview-0000001820879625-V5)组件。
> - HSP可以依赖其他HAR或HSP，但不支持循环依赖，也不支持依赖传递。


- **总结**
:::info
**hsp 和 har 区别？**
相同点：

1. 模块间资源共享
2. 不支持在设备上单独安装/运行
3. 不支持在配置文件中声明[UIAbility](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/uiability-overview-0000001774119974-V5)组件与[文档中心](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/extensionability-overview-0000001820879625-V5)组件
4. 不支持循环依赖，也不支持依赖传递

**不同点：**

1. har 发布二方或三方仓使用（hsp 不可以）
2. har 中不支持在配置文件中声明[pages](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/module-configuration-file-0000001820879553-V5#ZH-CN_TOPIC_0000001881258481__pages%E6%A0%87%E7%AD%BE)页面
3. hsp 中支持在配置文件中声明[pages](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/module-configuration-file-0000001820879553-V5#ZH-CN_TOPIC_0000001881258481__pages%E6%A0%87%E7%AD%BE)页面
4. hsp 多模块依赖只会打包一份
5. hsp 支持按需加载
:::

- 三层架构体验

步骤：

1. 选择文件-新建项目
![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1710929714153-8df732bb-3670-411c-a96f-67bcc9849ce6.png#averageHue=%23343d45&clientId=u2174620b-5394-4&from=paste&height=313&id=u9776aa02&originHeight=626&originWidth=886&originalType=binary&ratio=2&rotation=0&showTitle=false&size=234438&status=done&style=none&taskId=u043c231c-2164-4d60-bf22-48b3a5f4e39&title=&width=443)
2. 选择Empty Ability，然后 Next
![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1710929923702-b48ebc58-b129-4f31-836d-ef8c88561e4f.png#averageHue=%2332363d&clientId=u2174620b-5394-4&from=paste&height=663&id=u0062f186&originHeight=1326&originWidth=1992&originalType=binary&ratio=2&rotation=0&showTitle=false&size=170452&status=done&style=none&taskId=ucd20b82d-eddf-47e7-adc7-1b99ce863a3&title=&width=996)
3. 填写项目信息：项目名、包名、选择存储位置（注意带上项目名作为目录名）
![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1710930460950-a00089c7-f1f6-48ba-83c3-db6a25d3c6d7.png#averageHue=%2332383b&clientId=u2174620b-5394-4&from=paste&height=663&id=xMR57&originHeight=1326&originWidth=1992&originalType=binary&ratio=2&rotation=0&showTitle=false&size=165064&status=done&style=none&taskId=u5d096abe-2441-4ad4-8b0f-4a09488d5cc&title=&width=996)

演示包名：com.heima.elderly.pro

4. 新建common、features、 product 目录
5. 在 common 下新建一个叫 basic 公共模块 `Shared Library`包
![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1710992603080-3b922684-056a-446d-a29b-678415cdc5d9.png#averageHue=%2373869f&clientId=u5e90aad2-9d34-4&from=paste&height=663&id=ub3308f69&originHeight=1326&originWidth=1992&originalType=binary&ratio=2&rotation=0&showTitle=false&size=148074&status=done&style=none&taskId=u25ffb14d-af34-493f-9290-1f04f5a86ae&title=&width=996)
6. 在 features 下新建home、duty、 stage 、 mine、 message 、device业务模块 `Shared Library`包
7. **把默认 entry 包改名为 phone 放到product 目录下**

完成后目录结构，如图：
![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1710992659477-a6c5d0e3-cc70-4795-8ad5-8c34a0a77761.png#averageHue=%232b343c&clientId=u5e90aad2-9d34-4&from=paste&height=756&id=uca4eebfe&originHeight=1512&originWidth=736&originalType=binary&ratio=2&rotation=0&showTitle=false&size=124000&status=done&style=none&taskId=u97daa31d-171a-4b4e-8a02-83b9fc8bab1&title=&width=368)

8. 运行项目（修改运行调试配置）
# 项目运行
配置运行项目：

1. 选择编辑配置

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1711100143628-55754e18-d4df-4ba6-abf2-b46f09b55091.png#averageHue=%23323c47&clientId=u6d3c8c11-5249-4&from=paste&height=289&id=CwhMz&originHeight=578&originWidth=470&originalType=binary&ratio=2&rotation=0&showTitle=false&size=119933&status=done&style=none&taskId=ueadf5ebd-4422-4ba9-8fa3-a12926e8638&title=&width=235)

2. 安装依赖的动态共享包（Shared Library）

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1711100337171-1eb82e79-1464-4c34-9a57-78945fca603f.png#averageHue=%231e282c&clientId=u6d3c8c11-5249-4&from=paste&height=703&id=RzemC&originHeight=1406&originWidth=2080&originalType=binary&ratio=2&rotation=0&showTitle=false&size=236291&status=done&style=none&taskId=u3e7e29d3-1d81-4636-a440-fa30b16c23a&title=&width=1040)

3. 选择依赖模块

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1711361075759-84164283-ff79-4e98-a013-e36baa06b9b4.png#averageHue=%231d272c&clientId=u0b8167f6-2eb5-4&from=paste&height=703&id=VSEgw&originHeight=1406&originWidth=2080&originalType=binary&ratio=2&rotation=0&showTitle=false&size=181249&status=done&style=none&taskId=ua2022aad-0bc8-422f-8859-fb0de584c2d&title=&width=1040)
重新运行项目即可

**小结：**
分层设计的好处？
1. 为华为的手机、2in1等1+8全场景设备提供支持，实现了“[一次开发，多端部署](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/foreword-0000001774120758)”的开发理念
2. **产品定制层、基础特性层和公共能力层**，为开发者构建了一个清晰、高效、可扩展的设计架构
每层模块的作用？
1. common
2. features
3. product
模块资源如何共享？
1. 定义共享资源（arkts/arkui）
2. 入口导出
3. 使用模块在模块下oh-package.json5 中引入依赖并同步
:::success
说明：课程使用创建好的项目进行学习开发
:::

# 二次开发项目
> 二开项目说明：
> 1. 基础配置和数据
> 2. 静态页面资源

> 1. 基础公共模块：basic
> 2. 核心业务模块：home、duty、 stage 、 mine、 message 、device
> 3. 产品入口模块：phone
>    1. 入口页面 Index（tab 页签）：首页、负责老人、护理工作台、我的、设备管理（安装、绑定、控制、报警）

> 项目基础模版： [https://gitee.com/shuiruohanyu/yikang-elder-care](https://gitee.com/shuiruohanyu/yikang-elder-care)

- 拉取代码
```bash
$ git clone https://gitee.com/shuiruohanyu/yikang-elder-care
```
:::info
使用DevEcoStudio打开项目
:::

- 修改项目的包名和应用名
```diff
{
  "app": {
+    "bundleName": "xxxxxxxxx",
    "vendor": "example",
    "versionCode": 1000000,
    "versionName": "1.0.0",
    "icon": "$media:app_icon",
    "label": "$string:app_name"
  }
}
```
```diff
{
  "string": [
    {
      "name": "module_desc",
      "value": "模块描述"
    },
    {
      "name": "EntryAbility_desc",
      "value": "翼康养老-员工端"
    },
    {
      "name": "EntryAbility_label",
+      "value": "xxxxxx"
    }
  ]
}
```

- 选择phone模块，因为phone模块为hap包，作为设备的入口

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718075203971-8cf62a32-8fd0-431b-bc86-f50edda78290.png#averageHue=%2395ab30&clientId=u027e42b0-7d58-4&from=paste&height=376&id=uef4cef6d&originHeight=376&originWidth=1394&originalType=binary&ratio=1&rotation=0&showTitle=false&size=24542&status=done&style=none&taskId=uf31117dd-579b-4b02-af5b-64e804ae1e2&title=&width=1394)
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718075227915-eea3d446-8a07-4b86-9b22-92d7c987ee16.png#averageHue=%23f9db67&clientId=u027e42b0-7d58-4&from=paste&height=721&id=u048b01b1&originHeight=721&originWidth=332&originalType=binary&ratio=1&rotation=0&showTitle=false&size=109360&status=done&style=none&taskId=ud0fe95a9-ab28-4642-9772-7ed887c6042&title=&width=332)

- 在gitee上新建仓库
- 在本地代码进行初始化(先删除本地的.git文件夹-该文件夹是隐藏文件夹)
```bash
$ git init
```

- 提交代码(使用编辑器DevStudio)

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718087009366-b681a6cc-1e90-4a6c-a242-00c0df828137.png#averageHue=%2342814a&clientId=u6d2c3845-a72c-4&from=paste&height=1164&id=u18001ed7&originHeight=1736&originWidth=1086&originalType=binary&ratio=1&rotation=0&showTitle=false&size=67036&status=done&style=none&taskId=u692987a4-2ddd-437d-bf57-8104c46f472&title=&width=728)
# 模块和目录结构
> 模块说明和目录
> - common公共业务模块
> - features 业务模块：home首页、duty负责老人、 stage工作台 、 mine我的、 message消息 、device设备
> - product入口模块

- 目录规范

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1710993320893-b6f60a42-5cf1-4af7-ba6d-d27827976dc9.png#averageHue=%23293439&clientId=u5e90aad2-9d34-4&from=paste&height=365&id=u043563a2&originHeight=730&originWidth=1300&originalType=binary&ratio=2&rotation=0&showTitle=false&size=88942&status=done&style=none&taskId=ubbe08b7e-aabc-4ab9-9544-ade5010221b&title=&width=650)

- 依赖关系
> 模板中已配置好，无需再写

说明：
 1.  在模块的`oh-package.json5`文件的`dependencies `配置模块间的依赖关系
```json
{
  "license": "",
  "devDependencies": {},
  "author": "",
  "name": "phone",
  "description": "Please describe the basic information.",
  "main": "",
  "version": "1.0.0",
  "dynamicDependencies": {},
  "dependencies": {
    "@hm/basic": "file:../../common/basic",
    "@hm/public": "file:../../common/public",
    "@ec/home": "file:../../features/home",
    "@ec/duty": "file:../../features/duty",
    "@ec/stage": "file:../../features/stage",
    "@ec/mine": "file:../../features/mine"
  }
}
```

- 配置代码格式化

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718087076132-9be4b41e-2a44-4220-92f9-48ae33eab3dc.png#averageHue=%233e4145&clientId=u6d2c3845-a72c-4&from=paste&height=1424&id=u37e1beee&originHeight=1424&originWidth=1964&originalType=binary&ratio=1&rotation=0&showTitle=false&size=155324&status=done&style=none&taskId=u05f0bdf1-c5db-4389-b944-02b5a7939fc&title=&width=1964)
# 配置dayjs和日志插件
[OpenHarmony三方库中心仓](https://ohpm.openharmony.cn/#/cn/home)
> 模板中已有插件
> - @itheima/wing-care
> - dayjs
> - @abner/log
> - @ohos/axios

- 目前请求的几种方式
> - http-鸿蒙原生
> - axios-第三方和前端一样-量相对最大
> - ok-http-第三方和前端一样
> - rcp-鸿蒙原生-几乎相当于axios-刚退出没多久！！！！

- 安装依赖的方式
> ohpm install xxxx

- 配置日志和dayjs插件
```arkts
import { Log } from '@abner/log'
import dayjs from 'dayjs';
import 'dayjs/locale/zh-cn'

// 设置语言为中文
dayjs.locale('zh-cn');

Log.init({
  tag: "yk-yl",
  domain: 0xEC00,
  close: false,
  isHilog: true,
  showLogLocation: true,
  logSize: 200
})
```

- 在EntryAbility中引入，并替换ability日志打印的方法
```typescript
import "../utils/plugin"
import { Log } from '@abner/log';
```

- 替换日志打印方法
```arkts
import { AbilityConstant, UIAbility, Want } from '@kit.AbilityKit';
import { window } from '@kit.ArkUI';
import "../utils/plugin"
import { Log } from '@abner/log';

export default class EntryAbility extends UIAbility {
  onCreate(want: Want, launchParam: AbilityConstant.LaunchParam): void {
    Log.info('Ability onDestroy');
  }

  onDestroy(): void {
    Log.info('Ability onDestroy');
  }

  onWindowStageCreate(windowStage: window.WindowStage): void {
    // Main window is created, set main page for this ability
    Log.info('Ability onWindowStageCreate');

    windowStage.loadContent('pages/Index', (err, data) => {
      if (err.code) {
        Log.error('Failed to load the content. Cause: %{public}s' + JSON.stringify(err) ?? '');
        return;
      }
      Log.info('Succeeded in loading the content. Data: %{public}s' + JSON.stringify(data) ?? '');
    });
  }

  onWindowStageDestroy(): void {
    // Main window is destroyed, release UI related resources
    Log.info('Ability onWindowStageDestroy');
  }

  onForeground(): void {
    // Ability has brought to foreground
    Log.info('Ability onForeground');
  }

  onBackground(): void {
    // Ability has back to background
    Log.info('Ability onBackground');
  }
}
```
# axios基础封装
> 基础工具我们可以自己基于http的kit能力进行封装，比如小时达项目，但是企业中也有很大一部分采用第三方插件，比如axios，此次项目我们针对axios进行定制封装

[OpenHarmony三方库中心仓](https://ohpm.openharmony.cn/#/cn/detail/@ohos%2Faxios)

- 基础封装
```arkts
// 导出封装的axios的实例
import { Log } from '@abner/log'
import axios, { AxiosError, AxiosResponse, InternalAxiosRequestConfig } from '@ohos/axios'

// 创建一个axios的实例
const instance = axios.create({
  baseURL: 'https://zhyl-harmony.itheima.net', // 基础地址  http://www.ithiema.net + /a/b/c/d
  timeout: 20000 // 网络请求超时时间
})

// 两个拦截器
instance.interceptors.request.use((config: InternalAxiosRequestConfig) => {
  // config就是请求的参数 地址 以及一些头信息
  // 一般在这个位置统一注入token
  Log.info("1.请求参数：" + JSON.stringify(config))
  config.headers.Authorization =
    'eyJhbGciOiJIUzI1NiJ9..8PrMKBNrVVCfcI4DgICxp6oBQGqy46m3oglFQCiKtzw'
  return config // 返回的这个参数是必须的 axios就是用我们返回的这个config发送请求
}, (error: AxiosError) => {
  // 说明失败了
  Log.error("3.异常信息：" + JSON.stringify(error))
  return Promise.reject(error)
}) // 注册请求拦截器- 看看你是不是把参数带全了 有没有必要报个错

// Promise pending  fullfilled rejected
instance.interceptors.response.use((response: AxiosResponse) => {
  // reponse是服务器返回给我的结果
  // axios的默认返回选项是 包了一层data的数据
  // 判断 是否执行成功
  if (response.data && response.data.code !== 200) {
    // 说明执行出现问题 要提示错误 返回promise的异常
    AlertDialog.show({ message: response.data.msg }) // 提示错误
    return Promise.reject(new Error(response.data.msg))
  }
  Log.info("2.响应结果：" + JSON.stringify(response.data))
  return response.data.data // 直接返回我们需要的数据

}, (error: AxiosError) => {
  AlertDialog.show({ message: error.message }) // 弹出错误信息
  Log.error("3.异常信息：" + JSON.stringify(error))
  return Promise.reject(error)

}) // 注册响应拦截器- 数据回来，有些东西是不是没有必要啊 没有必要 就脱掉 去掉 删掉

export { instance }
```

- 在utils/index.ets中导出
```typescript
export * from './Request'
```
> 由于在根目录的Index.ets中，我们已经将utils整个模块导出，所以不需要再单独导出
> ![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718091187658-2146a74d-b19f-4dc0-9384-9fa3699f47af.png#averageHue=%23a6a26f&clientId=u6d2c3845-a72c-4&from=paste&height=872&id=ube33c563&originHeight=872&originWidth=1670&originalType=binary&ratio=1&rotation=0&showTitle=false&size=116100&status=done&style=none&taskId=uc66d38d2-4e6e-4035-a2a8-bfc8828b91d&title=&width=1670)

- 在ability中简单测试下
```typescript
import { instance } from '@hm/basic'
onCreate(want: Want, launchParam: AbilityConstant.LaunchParam): void {
    Log.info('Ability onDestroy');
    instance.get("/employee/user")
  }
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718091993707-63ba2299-74c8-4af1-9fad-3ba5b1d39fac.png#averageHue=%23392f2f&clientId=uffba82ce-66a1-4&from=paste&height=1008&id=u43b4eae9&originHeight=1008&originWidth=2902&originalType=binary&ratio=1&rotation=0&showTitle=false&size=234019&status=done&style=none&taskId=ue22eed36-baf6-4bcf-a659-8b45906aa8b&title=&width=2902)
> 因为我们现在还没有token，所以可以将下面的一个自主token临时写入到请求拦截器，测试返回的数据

```arkts
  config.headers.Authorization =
    'eyJhbGciOiJIUzI1NiJ9..8PrMKBNrVVCfcI4DgICxp6oBQGqy46m3oglFQCiKtzw'
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718092086052-60bd8fc5-7b7c-41cf-a156-880db8862fdd.png#averageHue=%23323131&clientId=uffba82ce-66a1-4&from=paste&height=642&id=uf2a66ce1&originHeight=642&originWidth=2182&originalType=binary&ratio=1&rotation=0&showTitle=false&size=140040&status=done&style=none&taskId=u17236aa9-dc07-4036-81c5-7441e6d3a8c&title=&width=2182)

- 请求成功

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718092110377-f24c64c6-deee-4a32-a94f-80512b20614b.png#averageHue=%232b2b2b&clientId=uffba82ce-66a1-4&from=paste&height=600&id=u5eea4204&originHeight=600&originWidth=2962&originalType=binary&ratio=1&rotation=0&showTitle=false&size=122070&status=done&style=none&taskId=u86c6bfa3-7cd5-4f29-bd60-f9ad47d93f8&title=&width=2962)
> 去除测试代码后，提交代码

# axios二次封装制定参数和响应体类型
> 目前返回的数据结构并没有明确类型指定，无法高效率处理数据，接下来我们对数据结构进行类型指定

- 需求分析

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1715073388009-8221ab77-0e1d-4317-839b-9873b2b78ed3.png#averageHue=%232b353a&clientId=u24735993-16ff-4&from=paste&height=331&id=u7b3b81a1&originHeight=662&originWidth=1212&originalType=binary&ratio=2&rotation=0&showTitle=false&size=148057&status=done&style=none&taskId=ua4401829-cae0-4633-ad38-34c8e5fb72d&title=&width=606)
> request<T = any, **R = AxiosResponse<T>**, D = any>(config: AxiosRequestConfig<D>): Promise<R>
> 1. 分析request请求泛型参数
> 2. **知道如何自定义response类型**


:::warning
目标：自定义 请求参数 和 响应结果 类型
:::

1. 指定请求参数类型
> - 请求参数：request(requestParams:RequestParams)
> - 响应结果：request<DataModel = null, DataParams = null>(...)

```arkts
import axios, { AxiosRequestConfig, AxiosError, AxiosResponse, InternalAxiosRequestConfig, } from '@ohos/axios';
// 导出统一的方法使用
// R是返回的类型 D是请求类型
export function request<Response = null, DataParams = null>(config: AxiosRequestConfig) {
  return instance<null, Response, DataParams>(config)
}
```

- 在ability中测试
> 测试

```arkts
import { request } from '@hm/basic'

onCreate(want: Want, launchParam: AbilityConstant.LaunchParam): void {
    Log.info('Ability onDestroy');

    interface UserData {
      /**
       * 头像
       */
      avatar: null | string;

      /**
       * 用户id
       */
      id: string;

      /**
       * 用户昵称
       */
      nickName: string;

      /**
       * 用户账号，即邮箱
       */
      username: string;
    }

    request<UserData>({
      url: '/employee/user'
    }).then(res => {
      Log.info("得到数据：" + JSON.stringify(res))
    })
  }
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718094166203-49a3b938-ba60-461d-b2de-4cd0cb8ca12f.png#averageHue=%23333333&clientId=uffba82ce-66a1-4&from=paste&height=430&id=ub404c899&originHeight=430&originWidth=2352&originalType=binary&ratio=1&rotation=0&showTitle=false&size=58669&status=done&style=none&taskId=u7925d131-1e85-4478-9c6c-1a822953561&title=&width=2352)
> 删除测试代码，提交代码

# Auth鉴权模块
> 使用首选项管理token的获取和写入

- 创建操作首选项的类并定义token 存储的 key和类型
```arkts
import preferences from '@ohos.data.preferences'

class UserAuthStore {
  KEY = 'user-store'
  context?: Context

  // 定义方法
  // 获取仓库
  getStore() {
    return preferences.getPreferences(this.context || getContext(), { name: 'UserStore' })
  }

  async setUser(data: User) {
    const store = await this.getStore()
    await store.put(this.KEY, JSON.stringify(data))
    await store.flush()
  }

  async getUser() {
    const store = await this.getStore()
    const data = await store.get(this.KEY, '{}') as string
    return JSON.parse(data) as User
  }

  async delUser() {
    const store = await this.getStore()
    await store.delete(this.KEY)
    await store.flush()
  }
}

export class User {
  token: string = ''
}

export const TokenManager = new UserAuthStore()
```

- utils入口统一导出
```arkts
export * from './Auth'
```
> 提交代码

**小结：**
用户首选项？用户首选项为应用提供Key-Value键值型的数据处理能力，支持应用持久化轻量级数据，并对其修改和查询。
支持的存储的数据类型？数据存储形式为键值对，键的类型为字符串型，值的存储数据类型包括数字型、字符型、布尔型以及这3种类型的数组类型。
# 广告弹窗创建

- 广告弹窗实现

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1710928097758-2a5ffef7-e38a-4c8e-9292-d44dee422e60.png#averageHue=%23e7c69a&clientId=u2174620b-5394-4&from=paste&height=640&id=OJ8UL&originHeight=1280&originWidth=589&originalType=binary&ratio=2&rotation=0&showTitle=false&size=3021920&status=done&style=none&taskId=ub76509af-5829-4520-b687-e987d3d8b4a&title=&width=294.5)

- 修改Ability默认跳转
```arkts
async onWindowStageCreate(windowStage: window.WindowStage): Promise<void> {
    // Main window is created, set main page for this ability
    Log.info('Ability onWindowStageCreate');

    Log.info('Ability onWindowStageCreate')
    const win = await windowStage.createSubWindow("ad_win")
    await win.showWindow()
    win.setUIContent("@bundle:com.heima.elderly.pro/home/ets/pages/Splash/Splash")
    windowStage.loadContent('pages/Index', (err, data) => {
      if (err.code) {
        Log.error('Failed to load the content. Cause: %{public}s' + JSON.stringify(err) ?? '');
        return;
      }
      Log.info('Succeeded in loading the content. Data: %{public}s' + JSON.stringify(data) ?? '');
    });
  }
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718095696022-1b60817f-133a-4c7e-8f4c-2d4368519a25.png#averageHue=%23e7b87f&clientId=uffba82ce-66a1-4&from=paste&height=721&id=ua34f61e7&originHeight=721&originWidth=333&originalType=binary&ratio=1&rotation=0&showTitle=false&size=369933&status=done&style=none&taskId=u2689d450-fce2-4104-a080-2ade82fc7de&title=&width=333)

- 在闪屏广告页进行处理-5秒后销毁窗口
```typescript
countdown() {
    this.timer = setInterval(() => {
      this.count--
      if (this.count === 0) {
        clearInterval(this.timer)
        window.findWindow("ad_win").destroyWindow()
        return
      }
    }, 1000)
  }
```
**小结：**
hsp 模块页面跳转？地址组成：@bundle:包名/模块名/ets/pages/页面名
包名哪里获取？AppScope/app.json5
最佳实践？统一封装管理路由地址

# 特性模块路由地址管理
> 很多页面都是跨包的页面
> 跨hsp页面跳转 @bundle:包名/模块名称/ets/pages/xxx

- 统一管理 feature 模块页面地址

[HarmonyOS_NEXT/MultiFinancialManagement/commons/basic/src/main/ets/constants/RouterConstants.ets · HarmonyOS/Codelabs - Gitee.com](https://gitee.com/harmonyos/codelabs/blob/master/HarmonyOS_NEXT/MultiFinancialManagement/commons/basic/src/main/ets/constants/RouterConstants.ets)

- 将所有地址统一管理在一个静态文件中
```arkts
export class RouterConfig {
  // 基础
  static BUNDLE: string = 'com.hm.elderly.plus'
  static readonly SPLASH_URL: string = `@bundle:${RouterConfig.BUNDLE}/home/ets/pages/Splash/Splash`
  static readonly LOGIN_URL: string = `@bundle:${RouterConfig.BUNDLE}/mine/ets/pages/Login/Login`
  static readonly USER_SET_URL: string = `@bundle:${RouterConfig.BUNDLE}/mine/ets/pages/Setting/Setting`
  static readonly USER_NAME_URL: string = `@bundle:${RouterConfig.BUNDLE}/mine/ets/pages/NickName/NickName`
  // 我的消息
  static readonly USER_MSG_URL: string = `@bundle:${RouterConfig.BUNDLE}/message/ets/pages/MsgPage`
  // 负责老人
  static readonly DUTY_HEALTH_DATA_URL: string = `@bundle:${RouterConfig.BUNDLE}/duty/ets/pages/Health/HealthList`
  static readonly DUTY_HEALTH_DETAIL_URL: string = `@bundle:${RouterConfig.BUNDLE}/duty/ets/pages/Health/HealthDetail`
  static readonly DUTY_HEALTH_RECORD_URL: string = `@bundle:${RouterConfig.BUNDLE}/duty/ets/pages/Health/HealthRecord`
  static readonly DUTY_CARE_TASK_URL: string = `@bundle:${RouterConfig.BUNDLE}/duty/ets/pages/Care/CareTask`
  // 护理工作台
  static readonly STAGE_NURSE_RECORDS_URL: string = `@bundle:${RouterConfig.BUNDLE}/stage/ets/pages/Nurse/NurseRecord`
  static readonly STAGE_EXECUTE_URL: string = `@bundle:${RouterConfig.BUNDLE}/stage/ets/pages/Execute/Execute`
  // 设备安装
  static readonly DEVICE_SETUP_URL: string = `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Device/SetupDevice`
  static readonly DEVICE_SETUP_TIP_URL: string = `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Device/SetupDeviceTip`
  static readonly DEVICE_ELDER_SEL_URL: string = `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Device/SelectElder`
  static readonly DEVICE_PRODUCT_SEL_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/device/ProductDevice`
  // 设备绑定
  static readonly DEVICE_BIND_URL: string = `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Device/BindDevice`
  static readonly DEVICE_BIND_TIP_URL: string = `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Device/BindDeviceTip`
  static readonly DEVICE_NEAR_URL: string = `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Device/NearDevice`
  static readonly DEVICE_BIND_BLOOD_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/BloodManage/BindBlood`
  // 我的设备
  static readonly DEVICE_MY_URL: string = `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Device/MyDevice`
  // 血糖
  static readonly DEVICE_BLOOD_TEST_PREPARE_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/BloodManage/TestPrepare`
  static readonly DEVICE_BLOOD_TEST_START_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/BloodManage/TestStart`
  static readonly DEVICE_BLOOD_TEST_RESULT_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/BloodManage/TestResult`
  // 门禁
  static readonly DEVICE_DOOR_NET_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/WIfi/NetDoor`
  static readonly DEVICE_DOOR_NET_CONNECT_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/WIfi/ConnectNet`
  static readonly DEVICE_DOOR_SCAN_WIFI_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/WIfi/ScanWifi`
  static readonly DEVICE_DOOR_CONNECT_TIP_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/WIfi/ConnectTip`
  // 控制
  static readonly DEVICE_DOOR_CONTROL_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Access/DoorControl`
  static readonly DEVICE_DOOR_BLUE_OPEN_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Access/BluetoothOpen`
  static readonly DEVICE_DOOR_REMOTE_OPEN_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Access/RemoteOpen`
  static readonly DEVICE_DOOR_PASSWORD_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Access/TempPassword`
  static readonly DEVICE_DOOR_OPEN_RECORD_URL: string =
    `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Access/OpenRecord`
  // 报警设备
  static readonly DEVICE_ALARM_URL: string = `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Alarm/AlarmDevice`
  static readonly DEVICE_ALARM_HANDLER_URL: string = `@bundle:${RouterConfig.BUNDLE}/device/ets/pages/Alarm/AlarmHandle`
}
```

- 在constants/Index.ets中统一导出
```typescript
export * from './RouterConfig'
```

2. Ability默认跳转使用
```arkts
async onWindowStageCreate(windowStage: window.WindowStage): Promise<void> {
    // Main window is created, set main page for this ability
    Log.info('Ability onWindowStageCreate')
    const win = await windowStage.createSubWindow("ad_win")
    await win.showWindow()
    win.setUIContent(RouterConfig.SPLASH_URL)
    windowStage.loadContent('pages/Index', (err, data) => {
      if (err.code) {
        Log.error('Failed to load the content. Cause: %{public}s' + JSON.stringify(err) ?? '');
        return;
      }
      Log.info('Succeeded in loading the content. Data: %{public}s' + JSON.stringify(data) ?? '');
    });
  }
```
> 提交代码

**小结：**
统一管理的好处？提高代码可维护性

# token判断跳转逻辑

- 判断有无 token 做跳转处理
1. 获取首选项中token 处理跳转
```arkts
async onWindowStageCreate(windowStage: window.WindowStage): Promise<void> {
  // Main window is created, set main page for this ability
  Log.info('Ability onWindowStageCreate')
  const win = await windowStage.createSubWindow("ad_win")
  await win.showWindow()
  win.setUIContent(RouterConfig.SPLASH_URL)
  TokenManager.context = this.context // 上下文
  if ((await TokenManager.getUser()).token) {
    windowStage.loadContent('pages/Index');
  } else {
    windowStage.loadContent(RouterConfig.LOGIN_URL);
  }

}

```
# 华为登录介绍
> Account Kit（华为帐号服务）提供简单、快速、安全的登录和授权功能，让用户无需输入帐号、密码和繁琐验证，避免因忘记密码而带来的麻烦，为您创建帐号并登录所有HarmonyOS应用和元服务，带来更高的注册转化，同时通过授权获得用户头像昵称、手机号码等信息，提升用户黏性。

[文档中心](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V4/account-introduction-0000001649652145-V4)

- 开发前准备
> #### AGC创建项目和应用

- 申请账号授权

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1720005894256-0e34e079-2837-4aa1-bd35-d2ab28f6a3f0.png#averageHue=%23fafafa&clientId=u5f83e791-fe50-4&from=paste&height=598&id=ua1b78748&originHeight=598&originWidth=1561&originalType=binary&ratio=1&rotation=0&showTitle=false&size=68449&status=done&style=none&taskId=u12b26b1f-0627-4181-8ad7-26637cbd74d&title=&width=1561)

[华为提供的面向开发者的应用一站式服务平台](https://developer.huawei.com/consumer/cn/service/josp/agc/index.html#/myProject)

- 配置应用签名证书指纹
1. 在DevEco Studio工具侧[生成密钥（.p12）和证书请求文件（.csr）](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/ide-publish-app-0000001053223745#section9752152162813)。

      注意：保存时填写.p12和.csr 后缀名

   1. ![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1715156518324-a974fe83-ecd3-4709-92a8-62c816d27550.png#averageHue=%232b353b&clientId=uc35cfd69-5961-4&from=paste&height=675&id=u143328a8&originHeight=1350&originWidth=964&originalType=binary&ratio=2&rotation=0&showTitle=false&size=305698&status=done&style=none&taskId=ubb829b34-90fd-4e2c-a323-ae537d5099a&title=&width=482)
   2. ![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1715156550805-589d42e7-3682-4535-a34b-7b169ce88a48.png#averageHue=%23202a2f&clientId=uc35cfd69-5961-4&from=paste&height=678&id=u55eabacd&originHeight=1356&originWidth=970&originalType=binary&ratio=2&rotation=0&showTitle=false&size=142098&status=done&style=none&taskId=u41278b7a-3042-4e0d-b3c9-f6f154c7011&title=&width=485)
   3. ![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1715156619509-4ee014cb-79d8-4058-95f7-00f2b5d16bc7.png#averageHue=%23212c30&clientId=uc35cfd69-5961-4&from=paste&height=335&id=u15ac350a&originHeight=670&originWidth=900&originalType=binary&ratio=2&rotation=0&showTitle=false&size=88107&status=done&style=none&taskId=ue1b9a828-18e2-4b84-b9cf-8f8f51b4b85&title=&width=450)
2. 请根据以下使用场景，在[AppGallery Connect](https://developer.huawei.com/consumer/cn/service/josp/agc/index.html)侧申请应用证书（.cer）、Profile（.p7b）文件等。

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1715157747742-a42f5277-8a6b-4597-b1a5-363a36d6b539.png#averageHue=%23faf8f7&clientId=uc35cfd69-5961-4&from=paste&height=187&id=u8d431824&originHeight=374&originWidth=1964&originalType=binary&ratio=2&rotation=0&showTitle=false&size=51399&status=done&style=none&taskId=u5c6dc935-e90c-4462-8525-ae904139d9d&title=&width=982)
![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1711530850663-24deb5d7-1441-43a6-8db3-474a47ccbb5c.png#averageHue=%23f9f8f8&clientId=u21dccb55-5c6d-4&from=paste&height=726&id=tIrQy&originHeight=1452&originWidth=2920&originalType=binary&ratio=2&rotation=0&showTitle=false&size=311020&status=done&style=none&taskId=ucde72dd9-a9bb-4aad-8a48-92248bf1627&title=&width=1460)
![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1715157646410-c6ed8c49-9832-46f9-8ccd-8eb8a65bbef4.png#averageHue=%23fbf9f9&clientId=uc35cfd69-5961-4&from=paste&height=373&id=u43f57749&originHeight=746&originWidth=1308&originalType=binary&ratio=2&rotation=0&showTitle=false&size=79954&status=done&style=none&taskId=u7fb2b671-80b7-448e-a169-da6e727ae0a&title=&width=654)

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1715158057162-cda94505-e131-4da9-97de-9c24fc7e3742.png#averageHue=%23f8f7f7&clientId=uc35cfd69-5961-4&from=paste&height=420&id=udd556ebb&originHeight=840&originWidth=2912&originalType=binary&ratio=2&rotation=0&showTitle=false&size=210415&status=done&style=none&taskId=u9ed9570c-a31b-4c36-989b-5aba0125040&title=&width=1456)

3. 最终获取的文件

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1715422062314-1c5febfa-a579-49c9-a960-95727292e15f.png#averageHue=%23252a2f&clientId=ua44f8a2b-f5fc-4&from=paste&height=322&id=u32a22570&originHeight=322&originWidth=1374&originalType=binary&ratio=1&rotation=0&showTitle=false&size=112289&status=done&style=none&taskId=u213527df-81dc-4f41-a2f7-8b8a0a2445f&title=&width=1374)

- 配置签名证书指纹

[文档中心](https://developer.huawei.com/consumer/cn/doc/app/agc-help-signature-info-0000001628566748#section5181019153511)
![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1711531269040-c16dd2a8-6556-453c-b46f-7e5560da3fdb.png#averageHue=%23f7f6f6&clientId=u21dccb55-5c6d-4&from=paste&height=697&id=F7CoH&originHeight=1394&originWidth=2720&originalType=binary&ratio=2&rotation=0&showTitle=false&size=745803&status=done&style=none&taskId=u0832206d-6de6-4257-933b-d2426d6676f&title=&width=1360)

3. 在DevEco Studio侧配置签名信息，请参考[配置签名信息](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/ide-publish-app-0000001053223745#section280162182818)。

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1711530254115-bf4a136a-10de-4396-9d52-9749dd43d035.png#averageHue=%23303237&clientId=u21dccb55-5c6d-4&from=paste&height=611&id=u5005e5ba&originHeight=1222&originWidth=1960&originalType=binary&ratio=2&rotation=0&showTitle=false&size=207251&status=done&style=none&taskId=u1cb7481f-207c-4de3-960f-394f8c3ec29&title=&width=980)

- 配置权限

[文档中心](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/account-client-id-0000001658658849)

1. 项目配置应用 clientId
```diff
{
  "module": {
    "name": "phone",
    "type": "entry",
    "description": "$string:module_desc",
    "mainElement": "EntryAbility",
    "metadata": [
      {
+       "name": "client_id",
+       "value": "110077743"
      }
    ],
    "deviceTypes": [
      "phone",
      "tablet",
      "2in1"
    ],
    "requestPermissions": [
      {
        "name": "ohos.permission.INTERNET"
      },
      {
        "name": "ohos.permission.ACCESS_BLUETOOTH",
        "reason": "$string:app_name",
        "usedScene": {
          "abilities": [ "EntryAbility"],
          "when":"inuse"
        }
      },
      {
        "name": "ohos.permission.GET_WIFI_INFO"
      },
      {
        "name": "ohos.permission.ACCELEROMETER"
      }
    ],
    "deliveryWithInstall": true,
    "installationFree": false,
    "pages": "$profile:main_pages",
    "abilities": [
      {
        "name": "EntryAbility",
        "srcEntry": "./ets/entryability/EntryAbility.ets",
        "description": "$string:EntryAbility_desc",
        "icon": "$media:icon",
        "label": "$string:EntryAbility_label",
        "startWindowIcon": "$media:startIcon",
        "startWindowBackground": "$color:start_window_background",
        "exported": true,
        "skills": [
          {
            "entities": [
              "entity.system.home"
            ],
            "actions": [
              "action.system.home"
            ]
          }
        ]
      }
    ]
  }
}
```

2. 申请手机号授权

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1711531534996-26f78ec1-32d8-476e-9cf6-f3534bf59039.png#averageHue=%23f8f7f6&clientId=u21dccb55-5c6d-4&from=paste&height=582&id=ud7b8e08f&originHeight=1164&originWidth=2898&originalType=binary&ratio=2&rotation=0&showTitle=false&size=282444&status=done&style=none&taskId=u0a94c25a-4863-4693-94c3-7e727f50c5d&title=&width=1449)

开发准备工作？

# 华为登录实现

- **mine模块**

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1711789175729-3751109b-e880-4891-9e6b-7534dd0b5cc7.png#averageHue=%23fcfafa&clientId=ubdbe7b53-08f0-4&from=paste&height=328&id=u05a2b147&originHeight=656&originWidth=564&originalType=binary&ratio=2&rotation=0&showTitle=false&size=32240&status=done&style=none&taskId=u455f8ac4-4dca-481f-a007-92e1cb21896&title=&width=282)
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718250542987-92313bc9-cb66-451a-a7d5-07e9e43207f8.png#averageHue=%23f9f9f9&clientId=uffba82ce-66a1-4&from=paste&height=1847&id=u538838b4&originHeight=1847&originWidth=2775&originalType=binary&ratio=1&rotation=0&showTitle=false&size=251705&status=done&style=none&taskId=u70bf0f5a-e596-4e18-96eb-7d091b1bc4e&title=&width=2775)
> #### 场景介绍
> Account Kit开放手机号快速验证能力，用于帮助应用向用户发起手机号申请，应用在满足《[常见类型移动互联网应用程序必要个人信息范围规定](http://www.cac.gov.cn/2021-03/22/c_1617990997054277.htm)》（对第三方网站的内容，华为不承担任何责任）中使用手机号的必要业务场景，经用户同意后，应用可获取手机号，为用户提供相应服务。
> 该能力与[手机号实时验证](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/account-phonenumber-realtime-0000001763084613)的区别为：
> 1. 手机号快速验证，对用户选择的华为帐号绑定的手机号或者新增的手机号进行验证，但不保证是实时验证，首次需要用户授权；
> 2. 手机号实时验证，对用户发起的每次请求，Account Kit均会对用户选择的手机号或者新增的手机号进行实时验证，以保证手机号的有效性。
> 
**说明**
> 应用获取手机号之前，需要完成“获取您的手机号”的scope权限申请，详情参见[scope权限申请手机号](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/account-client-id-0000001658658849#section132012717318)，scope审批未完成或未通过，将无法正常展示和获取用户授权手机号。

- 导入[authentication](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/account-api-authentication-0000001652386449)模块及相关公共模块。
```arkts
import { authentication } from '@kit.AccountKit';
import { util } from '@kit.ArkTS';
```

- 创建登录方法，绑定事件
```typescript
// 华为登录
login() {
    // 创建授权请求，并设置参数
    let authRequest = new authentication.HuaweiIDProvider().createAuthorizationWithHuaweiIDRequest();
    // 获取手机号需要传如下scope，传参数之前需要先申请对应scope权限,才能返回对应数据
    authRequest.scopes = ['phone'];
    // 获取code需传如下permission
    authRequest.permissions = ['serviceauthcode'];
    // 用户是否需要登录授权，该值为true且用户未登录或未授权时，会拉起用户登录或授权页面
    authRequest.forceAuthorization = true;
    // 用于防跨站点请求伪造，非空字符串即可
    authRequest.state = util.generateRandomUUID();
  }
```

- 调用[AuthenticationController](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/account-api-authentication-0000001652386449#section620452019185)对象的[executeRequest](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/account-api-authentication-0000001652386449#section164113311433)方法执行授权请求，并在Callback中处理授权结果，从授权结果中解析出Authorization Code。
```arkts
  // 华为登录
  async login() {
    // 创建授权请求，并设置参数
    let authRequest = new authentication.HuaweiIDProvider().createAuthorizationWithHuaweiIDRequest();
    // 获取手机号需要传如下scope，传参数之前需要先申请对应scope权限,才能返回对应数据
    authRequest.scopes = ['phone'];
    // 获取code需传如下permission
    authRequest.permissions = ['serviceauthcode'];
    // 用户是否需要登录授权，该值为true且用户未登录或未授权时，会拉起用户登录或授权页面
    authRequest.forceAuthorization = true;
    // 用于防跨站点请求伪造，非空字符串即可
    authRequest.state = util.generateRandomUUID();
    try {
      let controller = new authentication.AuthenticationController(getContext(this));
      const result = await controller.executeRequest(authRequest) as authentication.AuthorizationWithHuaweiIDResponse;
      let state = result.state;
      if (state && authRequest.state != state) {
        return promptAction.showToast({ message: JSON.stringify(result) });
      }
      let authorizationWithHuaweiIDCredential = result.data!;
      let code = authorizationWithHuaweiIDCredential.authorizationCode;
      // 开发者处理code

    } catch (error) {
      promptAction.showToast({ message: error.message });

    }
  }
```
> #### 服务端开发
> **后台配合最终完成开发**
> **注意：华为认证需要后台配合华为认证登录完成手机号检查和注册和返回登录结构**

- 封装登录 api 请求函数
> 统一在 **common/basic **全局封装

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718251053742-82114bd9-762a-4a90-ad8e-7758472e9085.png#averageHue=%233a3d41&clientId=uffba82ce-66a1-4&from=paste&height=425&id=u35168be3&originHeight=624&originWidth=796&originalType=binary&ratio=1&rotation=0&showTitle=false&size=50753&status=done&style=none&taskId=ud81d6cff-e7ad-4f20-ad1d-df5de804db3&title=&width=542)

- 定义用户的类型
```typescript
// 登录：参数
export interface LoginParams {
  /**
   * 华为应用id
   */
  clientId?: null | string;

  /**
   * 华为应用秘钥
   */
  clientSecret?: null | string;

  /**
   * 华为账号授权码
   */
  code: string;
}
```

- 在viewmodel/index.ets中导出
```typescript
export * from './UserModel'
```

- 在basic模块封装用户API
```arkts
import { LoginParams } from '../viewmodel'
import { request } from '../utils'

/**
 * 华为手机号授权登录
 * @param data
 * @returns
 */
export function loginApi(data: LoginParams): Promise<string> {
  return request<string, LoginParams>({ url: '/employee/login', method: 'post', data })
}
```

- 在api/index.ets导出
```typescript
export * from './UserApi'
```

- 在basic/Index.ets中导出
```typescript
export * from './src/main/ets/api'

export * from './src/main/ets/viewmodel'
```

- mine模块调用登录接口并存储token
```arkts
  async login() {
    // 创建授权请求，并设置参数
    let authRequest = new authentication.HuaweiIDProvider().createAuthorizationWithHuaweiIDRequest();
    // 获取手机号需要传如下scope，传参数之前需要先申请对应scope权限,才能返回对应数据
    authRequest.scopes = ['phone']; // 快速登录手机号
    // 获取code需传如下permission
    authRequest.permissions = ['serviceauthcode'];
    // 用户是否需要登录授权，该值为true且用户未登录或未授权时，会拉起用户登录或授权页面
    authRequest.forceAuthorization = true;
    // 用于防跨站点请求伪造，非空字符串即可
    authRequest.state = util.generateRandomUUID();
    try {
      let controller = new authentication.AuthenticationController(getContext(this)); // 创建拉起授权请求的controller
      const result = await controller.executeRequest(authRequest) as authentication.AuthorizationWithHuaweiIDResponse;
      if (authRequest.state !== result.state) {
        return promptAction.showToast({ message: '华为认证登录失败' })
      }
      const code = result.data?.authorizationCode! // 拿到真正的code
      const token = await loginApi({
        code,
        clientId: '111230731', // 实际情况下决定不能写
        clientSecret: '44c2ecbcc275ef10d1a7c6348d8a4695dc53f1f7ff87677c92ecdc29564ca93d' // 实际情况下决定不能写
      })
      // 写入首选项
      await TokenManager.setUser({ token })
      promptAction.showToast({ message: '登录成功' })
      router.replaceUrl({
        url: "pages/Index" // 跳转主页
      })

    } catch (error) {
      promptAction.showToast({ message: error.message })
    }


  }

```

- 测试

登录成功后，杀掉应用，再次进入闪屏后直接跳转首页
> **坑点：目前API11的项目才可以使用华为登录认证，API12的项目会提示指纹证书不正确，切记！！！**


**小结：**
手机号快速登录流程？
# axios拦截器注入token并处理401状态码token失效
> **basic模块**

- 请求拦截器中，统一添加请求头token
```arkts
// 添加请求拦截器
instance.interceptors.request.use(async (config: InternalAxiosRequestConfig) => {
  // 对请求数据做点什么
  // config.headers.Authorization =
  //   'eyJhbGciOiJIUzI1NiJ9..8PrMKBNrVVCfcI4DgICxp6oBQGqy46m3oglFQCiKtzw'
  Log.info(`1-请求配置${config.method!}-${config.url}：${JSON.stringify(config.params || config.data)}`)
  const userToken = await TokenManager.getUser()
  if (userToken.token) {
    config.headers.Authorization = userToken.token
  }
  return config;
}, (error: AxiosError) => {
  // 对请求错误做些什么
  return Promise.reject(error);
});

```

- 响应拦截器，处理 401
```arkts
// Promise pending  fullfilled rejected
instance.interceptors.response.use((response: AxiosResponse) => {
  // reponse是服务器返回给我的结果
  // axios的默认返回选项是 包了一层data的数据
  // 判断 是否执行成功
  if (response.data && response.data.code !== 200) {
    // 说明执行出现问题 要提示错误 返回promise的异常
    AlertDialog.show({ message: response.data.msg }) // 提示错误
    return Promise.reject(new Error(response.data.msg))
  }
  Log.info("2.响应结果：" + JSON.stringify(response.data))
  return response.data.data // 直接返回我们需要的数据

}, (error: AxiosError) => {
  // 4xx 5xx会走到axios的响应拦截器的错误回调函数中
  if (error.response?.status === 401) {
    // 说明登录失效了 因为有效期为180天 没必要重新用复活 直接重新登录即可
    TokenManager.delUser() // 删除原有token
    router.replaceUrl({
      url: RouterConfig.LOGIN_URL
    })
  } else {
    // 除401之外的错误可以弹出显示
    AlertDialog.show({ message: error.message }) // 弹出错误信息
  }
  //
  Log.error("3.异常信息：" + JSON.stringify(error))
  return Promise.reject(error)

}) // 注册响应拦截器- 数据回来，有些东西是不是没有必要啊 没有必要 就脱掉 去掉 删掉
```

# 设备管理builder抽提
> 设备管理结构复用

- 复用首页设备管理样式

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1711790786005-3d870c9a-fb94-4b17-ac8c-a706c35fbbd1.png#averageHue=%237ccfc5&clientId=u52f6cc51-0888-4&from=paste&height=150&id=ucdd2468d&originHeight=300&originWidth=650&originalType=binary&ratio=2&rotation=0&showTitle=false&size=38193&status=done&style=none&taskId=u589246b3-b051-455a-895a-3dba96f5c43&title=&width=325)
> 我的页签中同样也需要这样四个的卡片，将上个小节封装的好的内容提到basic中，这样不论是mine模块还是
> 我的页面都可以非常轻松的实现同样的效果

- 将features中Home的设备管理的样式抽提到basic中的builders目录下

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718274859321-9627d34c-fc6a-4c89-bfad-fb6bf0d0e27e.png#averageHue=%233c3f42&clientId=u9bd24110-2897-4&from=paste&height=808&id=u9653c9c2&originHeight=808&originWidth=748&originalType=binary&ratio=1&rotation=0&showTitle=false&size=64002&status=done&style=none&taskId=u9b4a03e4-d59a-4fed-b367-c97fe6b52d6&title=&width=748)

- 拷贝图片资源 

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718283225293-3b834c4f-1988-4de9-b139-ee833e9c3838.png#averageHue=%233f4143&clientId=u9bd24110-2897-4&from=paste&height=324&id=u103ef439&originHeight=324&originWidth=504&originalType=binary&ratio=1&rotation=0&showTitle=false&size=40973&status=done&style=none&taskId=ubc1a2f9e-902c-42ee-bbee-7801b013dfa&title=&width=504)

- 抽提公共的builder
```typescript
import { ResManager } from '../utils'
import { router } from '@kit.ArkUI'
import { RouterConfig } from '../constants'

class CareBuilderParams {
  title: string = ""
  icon: ResourceStr = ""
  direction: "horizontal" | "vertical" = "horizontal" // 排列方式
  pageUrl: string = "" // 跳转地址
}

// 模块item
@Builder
/**
 *
 * @param title
 * @param icon
 * @param pos
 * @param url
 */
function careItem(item: CareBuilderParams) {
  if (item.direction === "horizontal") {
    // 护理工作台
    Row({ space: 10 }) {
      Image(item.icon)
        .width(48)
        .aspectRatio(1)
      Text(item.title)
        .fontColor(ResManager.EC_MID_19)
        .fontSize(ResManager.EC_NORMAL_FS)
    }
    .padding({
      top: 20,
      bottom: 23,
      left: 13,
      right: 13
    })
    // .borderRadius(8)
    // .layoutWeight(1)
    // .backgroundColor(ResManager.EC_MID_WHITE)
    .modStyle()
    .onClick(() => {
      item.pageUrl && router.pushUrl({
        url: item.pageUrl
      })
    })
  } else {
    // 设备管理
    Column({ space: 10 }) {
      Image(item.icon)
        .width(40)
        .aspectRatio(1)
      Text(item.title)
        .fontColor(ResManager.EC_MID_19)
        .fontSize(ResManager.EC_SMALL_FS)
    }
    .padding({ top: 23, bottom: 23 })
    // .borderRadius(8)
    // .layoutWeight(1)
    // .backgroundColor(ResManager.EC_MID_WHITE)
    .modStyle()
    .onClick(() => {
      item.pageUrl && router.pushUrl({
        url: item.pageUrl
      })
    })
  }
}

// 可复用样式
@Styles
function modStyle() {
  .borderRadius(8)
  .layoutWeight(1)
  .backgroundColor(ResManager.EC_MID_WHITE)
}

@Builder
export function DeviceBuilder() {
  Row({ space: 10 }) {
    careItem({
      title: '设备安装',
      icon: $r('app.media.icon_home_sbaz'),
      direction: 'vertical',
      pageUrl: RouterConfig.DEVICE_SETUP_URL
    })
    careItem({
      title: '设备绑定',
      icon: $r('app.media.icon_home_sbkz'),
      direction: 'vertical',
      pageUrl: RouterConfig.DEVICE_BIND_URL
    })
    careItem({
      title: '我的设备',
      icon: $r('app.media.icon_home_wdsb'),
      direction: 'vertical',
      pageUrl: RouterConfig.DEVICE_MY_URL
    })
    careItem({
      title: '报警设备',
      icon: $r('app.media.icon_home_bjsb'),
      direction: 'vertical',
      pageUrl: RouterConfig.DEVICE_ALARM_URL
    })
  }
  .width('100%')
  .justifyContent(FlexAlign.SpaceBetween)
}

@Builder
export function CareBuilder() {
  Row({ space: 10 }) {
    careItem({
      title: '护理任务',
      icon: $r('app.media.icon_home_hlrw'),
      direction: 'horizontal',
      pageUrl: ""
    })
    careItem({
      title: '护理记录',
      icon: $r('app.media.icon_home_hljl'),
      direction: 'horizontal',
      pageUrl: ""
    })
  }
  .width('100%')
  .justifyContent(FlexAlign.SpaceBetween)
}
```

- 在basic/builders/index.ets导出
```typescript
export * from './DeviceCardBuilder'
```

- 统一在basic/Index.ets中导出
```typescript
export * from './src/main/ets/builders' 
```

- 在Home组件中应用

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718284416327-a2ccc6f0-56b8-434b-b849-505b78681329.png#averageHue=%232f2e2d&clientId=u9bd24110-2897-4&from=paste&height=1012&id=uf5a70ab5&originHeight=1012&originWidth=1450&originalType=binary&ratio=1&rotation=0&showTitle=false&size=104114&status=done&style=none&taskId=ufa537b09-6899-4eef-86be-2323742807b&title=&width=1450)

- 在Mine组件中应用

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718284574673-f3f396c5-6042-4238-be2b-48f8ca2e9f96.png#averageHue=%232e2e2e&clientId=u9bd24110-2897-4&from=paste&height=1002&id=u982b7859&originHeight=1002&originWidth=1940&originalType=binary&ratio=1&rotation=0&showTitle=false&size=127647&status=done&style=none&taskId=ubb6e2f3b-c64e-43ac-9643-7d579986bef&title=&width=1940)
> 提交代码


# 用户信息-获取

- 获取登录护理员信息

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1711790867674-87412baf-6c39-40a6-a13e-196018208d6f.png#averageHue=%23deeff3&clientId=u52f6cc51-0888-4&from=paste&height=658&id=uc5d8a13d&originHeight=1316&originWidth=690&originalType=binary&ratio=2&rotation=0&showTitle=false&size=357936&status=done&style=none&taskId=u3c34c6db-64ab-4068-bf10-cd65a1bb245&title=&width=345)
> **basic模块**

- 定义用户数据类型
```typescript
// 我的：登录用户
export interface UserData {
  /** 头像 */
  avatar: null | string;

  /** 用户id */
  id: string;

  /** 用户昵称 */
  nickName: string;

  /** 用户账号，即邮箱 */
  username: string;
}
export class UserDataModel implements UserData {
  avatar: null | string = null
  id: string = ''
  nickName: string = ''
  username: string = ''

  constructor(model: UserData) {
    this.avatar = model.avatar
    this.id = model.id
    this.nickName = model.nickName
    this.username = model.username
  }
}

```

- 封装 api 函数
```arkts
/**
 * 获取登录用户信息
 * @returns
 */
export function getUserApi(): Promise<UserDataModel> {
  return request<UserDataModel>({ url: '/employee/user' })
}
```
> 在mine模块中获取信息
> - 声明响应式数据
> - 初始化获取数据
> - 更新UI显示

```arkts
 @State
  user: UserDataModel = new UserDataModel({} as UserData)

  async getUser() {
    const data = await getUserApi()
    this.user = data
  }

  aboutToAppear(): void {
    this.getUser()
  }
```
```arkts
 Column({ space: 5 }) {
          Image(this.user.avatar ||
            'https://img2.baidu.com/it/u=130573784,2479509663&fm=253&fmt=auto&app=138&f=JPEG?w=500&h=534')
            .width(74)
            .aspectRatio(1)
            .borderRadius(37)
            .border({ width: 5, color: '#AEE8E4' })
            .shadow({
              offsetX: 0,
              offsetY: 2,
              radius: 21,
              color: '#0aa9941f'
            })
          Text(this.user.nickName || "-")
            .fontSize(ResManager.EC_PAGE_TITLE_FS)
            .fontColor(ResManager.EC_MID_BLACK)
            .fontWeight(500)
          Text(this.user.username || "-")
            .fontSize(13)
            .fontColor(ResManager.EC_MID_59)

        }
        .margin({ top: 64 - this.topHeight })
```

- 点击个人信息区域跳转设置详情页
```arkts
 Column({ space: 5 }) {
          Image(this.user.avatar ||
            'https://img2.baidu.com/it/u=130573784,2479509663&fm=253&fmt=auto&app=138&f=JPEG?w=500&h=534')
            .width(74)
            .aspectRatio(1)
            .borderRadius(37)
            .border({ width: 5, color: '#AEE8E4' })
            .shadow({
              offsetX: 0,
              offsetY: 2,
              radius: 21,
              color: '#0aa9941f'
            })
          Text(this.user.nickName || "-")
            .fontSize(ResManager.EC_PAGE_TITLE_FS)
            .fontColor(ResManager.EC_MID_BLACK)
            .fontWeight(500)
          Text(this.user.username || "-")
            .fontSize(13)
            .fontColor(ResManager.EC_MID_59)

        }
        .margin({ top: 64 - this.topHeight })
        .onClick(() => {
          router.pushUrl({
            url: RouterConfig.USER_SET_URL
          })
        })
```

> 提交代码


# 用户信息-编辑器页获取信息
> - 修改昵称
> - 上传修改头像

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1711790892525-74ae9ac4-01c7-4a12-a32c-fc5d031a2caa.png#averageHue=%23bfefab&clientId=u52f6cc51-0888-4&from=paste&height=237&id=d1n7E&originHeight=474&originWidth=640&originalType=binary&ratio=2&rotation=0&showTitle=false&size=30562&status=done&style=none&taskId=u29f9ecf4-20fc-4379-8b38-5bd5965106e&title=&width=320)
> 首先获取用户的信息

- 获取用户信息（和我的组件一致）
```arkts
 @State
  user: UserDataModel = new UserDataModel({} as UserData)

  async getUser() {
    this.user = await getUserApi()
  }

  aboutToAppear(): void {
    this.getUser()
  }

```
> 还可以在跳转的时候通过传递参数的形式将整个用户对象传递过去

如
```typescript
  router.pushUrl({
            url: RouterConfig.USER_SET_URL,
            params: this.user
          })
```
需要在aboutToAppear获取
```typescript
 @State
  user: UserDataModel = new UserDataModel({} as UserData)

  aboutToAppear(): void {
    const params = router.getParams() as UserDataModel
    if (params) {
      // 一定得判断 因为鸿蒙里面没有传值的情况下 params是个undefined
      this.user = params
    }
  }
```

- 定义一个枚举类型
```typescript
enum ShowType {
  NICKNAME,
  AVATAR
}

```

- 更新UI
```arkts
  @Builder
  cellItem(title: string, type: ShowType) {
    Row() {
      Text(title)
        .fontColor(ResManager.EC_MID_19)
        .fontSize(ResManager.EC_MODULE_TITLE_FS)
        .fontWeight(500)
      Row({ space: 10 }) {
        if (type === ShowType.NICKNAME) {
          // 昵称修改
          Text(this.user.nickName || "-")
            .fontColor(ResManager.EC_MID_59)
            .fontSize(ResManager.EC_NORMAL_FS)
            .onClick(() => {
              router.pushUrl({
                url: 'pages/mine/NickNamePage'
              })
            })
        } else if (type === ShowType.AVATAR) {
          // 上传
          Image(this.user.avatar ||
            'https://img2.baidu.com/it/u=130573784,2479509663&fm=253&fmt=auto&app=138&f=JPEG?w=500&h=534')
            .width(34)
            .aspectRatio(1)
            .borderRadius(17)
            .onClick(() => {
              this.uploadDialog.open()
            })
        }
        Image($r('app.media.ic_arrow_black'))
          .width(7)
          .height(16)
      }
    }
    .width('100%')
    .padding({ top: 14, bottom: 14 })
    .justifyContent(FlexAlign.SpaceBetween)
  }
```

- 修改调用的位置
```typescript
  ListItem() {
              this.cellItem('头像', ShowType.NICKNAME)
            }

            ListItem() {
              this.cellItem('昵称', ShowType.AVATAR)
            }
```

# 唤起拍照-唤起相册-取消弹窗
> 上传组件已经在模板中**basic模块**准备好，我们需要进一步实现上传功能
> 组件位置：**common/basic/src/main/ets/components/EcDialogUpload.ets**

1. 点击拍照调用`cameraPicker.pick`
```arkts
ListItem() {
          Text('拍照')
            .fontColor(ResManager.EC_MAIN_COLOR)
            .fontSize(ResManager.EC_MODULE_TITLE_FS)
            .fontWeight(500)
            .width('100%')
            .textAlign(TextAlign.Center)
            .padding(15)
        }
        .onClick(() => {
          this.takePhoto()
        })

```
```arkts
  // 拍照
  async takePhoto() {
    let mContext = getContext(this) as common.Context;
    try {
      let pickerProfile: cameraPicker.PickerProfile = {
        cameraPosition: camera.CameraPosition.CAMERA_POSITION_BACK
      };
      let pickerResult: cameraPicker.PickerResult = await cameraPicker.pick(mContext,
        [cameraPicker.PickerMediaType.PHOTO, cameraPicker.PickerMediaType.VIDEO], pickerProfile);
      console.log("the pick pickerResult is:" + JSON.stringify(pickerResult));
      Log.info(pickerResult.resultUri)

    } catch (error) {
      let err = error as BusinessError;
      console.error(`the pick call failed. error code: ${err.code}`);
    }
  }
```

- 点击相册选择调用`picker.PhotoViewPicker`
```typescript
   ListItem() {
          Text('从相册选择')
            .fontColor(ResManager.EC_MAIN_COLOR)
            .fontSize(ResManager.EC_MODULE_TITLE_FS)
            .fontWeight(500)
            .width('100%')
            .textAlign(TextAlign.Center)
            .padding(15)
        }
        .onClick(() => {
          this.selectPhoto()
        })
```

- 实现选择相册方法
```arkts
 // 选择相册
  async selectPhoto() {
    // 选择文件
    const photoSelectOptions = new picker.PhotoSelectOptions()
    photoSelectOptions.MIMEType = picker.PhotoViewMIMETypes.IMAGE_TYPE
    photoSelectOptions.maxSelectNumber = 1

    const photoViewPicker = new picker.PhotoViewPicker()
    const result = await photoViewPicker.select(photoSelectOptions)
    // 拿到照片

  }
```

- 点击取消关闭弹层
```typescript
  ListItem() {
          Text('取消')
            .fontColor(ResManager.EC_MID_59)
            .fontSize(ResManager.EC_MODULE_TITLE_FS)
            .fontWeight(500)
            .width('100%')
            .textAlign(TextAlign.Center)
            .padding(15)
        }
        .onClick(() => {
          this.controller.close()
        })
```
> 提交代码

# 完成头像上传
> 4.0的上传巨大的坑点- 无法得到上传之后的地址-上传请求走完，拿不到返回数据
> Next版本中进行处理-onheaderReceive 回调中可以拿到返回数据
> 多线程上传- 致命问题-所有的代码均不能出现的闭包-几乎所有的第三方库 或者自己封装 都或多或少有闭包

```typescript
function test () {
   let a = 1 //被闭包了。gc
   const add = (b: number) => {
     return a + b
   }
  return add
}
```

> **basic模块**

- 在上传组件定义回调函数
```typescript
  callback: (url: string) => void = () => {
  } // 定义回调
```

- 上传组件通过回调将图片地址传出
```typescript
// 拍照
  async takePhoto() {
    let mContext = getContext(this) as common.Context;
    try {
      let pickerProfile: cameraPicker.PickerProfile = {
        cameraPosition: camera.CameraPosition.CAMERA_POSITION_BACK
      };
      let pickerResult: cameraPicker.PickerResult = await cameraPicker.pick(mContext,
        [cameraPicker.PickerMediaType.PHOTO, cameraPicker.PickerMediaType.VIDEO], pickerProfile);
      if (pickerResult.resultUri) {
        this.callback(pickerResult.resultUri) // 通过回调传出
      }

    } catch (error) {
      let err = error as BusinessError;
      console.error(`the pick call failed. error code: ${err.code}`);
    }
  }
```
```typescript
// 选择相册
  async selectPhoto() {
    // 选择文件
    const photoSelectOptions = new picker.PhotoSelectOptions()
    photoSelectOptions.MIMEType = picker.PhotoViewMIMETypes.IMAGE_TYPE
    photoSelectOptions.maxSelectNumber = 1

    const photoViewPicker = new picker.PhotoViewPicker()
    const result = await photoViewPicker.select(photoSelectOptions)
    // 拿到照片
    if (result.photoUris?.length) {
      this.callback(result.photoUris[0]) // 通过回调传出
    }
  }
```

- 当选择图片之后，将选择的图片通过组件的回调函数传递到父组件
```arkts
// 上传弹层控制
uploadDialog: CustomDialogController = new CustomDialogController({
  // 使用自定义弹层组件
  builder: EcDialogUpload({
    callback: (url) => {

    }
  }),
  alignment: DialogAlignment.Bottom
})
```
> 图片准备好之后，将获取的图片使用接口上传得到上传成功的地址

- 封装上传 api 函数
> **basic模块**

:::info
在basic模块下的api目录中新建一个Common.ets文件
:::
```arkts
// 使用axios上传 直接调用方法即可 不需要使用upload
import { FormData } from '@ohos/axios'

import { request } from '../utils/Request'

// 上传接口
export function uploadApi(data: FormData): Promise<string> {
  return request<string, FormData>({
    url: '/common/upload',
    method: 'post',
    headers: {
      "Content-Type": "multipart/form-data"
    },
    data,
    context: getContext() // 上下文
  })
}
```

- 在api/index.ets中进行统一导出
```typescript
export * from './Common'
```
> 因为上传文件会考虑到网络及图片大小等因素，所以为了不出现空白，可以在上传间隙弹出一个进度条，等到上传完毕，将进度条进行关闭

- 声明一个自定义弹窗的进度条，使用自己封装的第三方组件库的EcLoading组件
```arkts
// 全屏 loading 弹层
  loading: CustomDialogController = new CustomDialogController({
    builder: EcLoading({ tip: '上传中...' }),
    customStyle: true,
    autoCancel: false,
    alignment: DialogAlignment.Center
  })

  upload(uri: string) {
    try {
      this.loading.open()
      
    
    } catch (e) {
      this.loading.close()
      Log.error(e)
    }
  }
```

- 实现一个上传方法-在上传开始时打开进度条


- 实现上传方法
> 首选要将图片进行沙箱文件处理，因为上传接口只能接受沙箱文件的内容

```typescript
 // 上传方法
  async upload(url: string) {
    if (url) {
      // 1.拷贝到沙箱 2.调用上传接口 得到上传之后的地址
      const file = fileIo.openSync(url, fileIo.OpenMode.READ_ONLY)
      // 生成一个目标文件的路径
      const destPath = getContext().cacheDir + "/" + file.name
      // interal://cache:
      fileIo.copyFileSync(file.fd, destPath) // 拷贝到沙箱
      const data = new FormData()
      // 加上file的参数
      data.append("file", `internal://cache/${file.name}`)
      const res = await uploadApi(data) // 只是传到服务器了 但是并没有更改头像
      // 修改头像地址
      await updateUserAvatar(res) // 修改用户头像
      promptAction.showToast({ message: '修改用户头像成功' })
      this.uploadDialog.close() // 关闭弹层
    }
  }
```
> 请注意： 目前我们只是上传到服务器一张图片，改掉了在当前本地的头像，但是我们个人信息的真实地址并没有发生变化，此时需要进行调用接口更新


- 封装修改用户头像的API(-basic模块)
```typescript
/**
 * 更新用户头像
 * @param avatar
 * @returns
 */
export function updateUserPhotoApi(avatar: string): Promise<null> {
  return request<null, UpdateUserParamsModel>({
    url: '/employee/user/updateAvatar', method: 'put', params: {
      avatar
    }
  })
}
```

- 在上传图片成功之后调用接口进行修改
```typescript
 // 上传方法
  async upload(url: string) {
    if (url) {
      // 1.拷贝到沙箱 2.调用上传接口 得到上传之后的地址
      const file = fileIo.openSync(url, fileIo.OpenMode.READ_ONLY)
      // 生成一个目标文件的路径
      const destPath = getContext().cacheDir + "/" + file.name
      // interal://cache:
      fileIo.copyFileSync(file.fd, destPath) // 拷贝到沙箱
      const data = new FormData()
      // 加上file的参数
      data.append("file", `internal://cache/${file.name}`)
      const res = await uploadApi(data) // 只是传到服务器了 但是并没有更改头像
      // 修改头像地址
      await updateUserAvatar(res) // 修改用户头像
      promptAction.showToast({ message: '修改用户头像成功' })
      this.uploadDialog.close() // 关闭弹层
    }
  }
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718772756881-d23272b7-65be-49b1-b359-e7f5f5a4d533.png#averageHue=%2376837b&clientId=u370a3196-cfb1-4&from=paste&height=721&id=u0625cfe3&originHeight=1442&originWidth=664&originalType=binary&ratio=2&rotation=0&showTitle=false&size=80186&status=done&style=none&taskId=ub5ea938c-80d0-4769-bcf8-5a2a77572b6&title=&width=332)

> 提交代码

# 修改用户昵称

- 跳转修改昵称页面，传递当前昵称参数
```arkts
     Text(this.user.nickName || "-")
            .fontColor(ResManager.EC_MID_59)
            .fontSize(ResManager.EC_NORMAL_FS)
            .onClick(() => {
              router.pushUrl({
                url: RouterConfig.USER_NAME_URL,
                params: {
                  name: this.user.nickName
                }
              })
            })
```

- 昵称页面接收参数并回显
```arkts
 @State
  nickName: string = ""

  aboutToAppear(): void {
    const params = router.getParams() as object
    if (params && params["name"]) {
      this.nickName = params["name"]
    }
  }

// Text(this.nickname)
TextInput({ text: $$this.nickname, placeholder: '请输入昵称' })
  .height(50)
  .borderRadius(ResManager.EC_NORMAL_RD)
  .backgroundColor('#fff')
  .placeholderColor(ResManager.EC_MID_99)
```

- 根据昵称值有无对提交按钮控制
```arkts
Button('保存', { type: ButtonType.Capsule })
  .margin({ top: 50 })
  .width(230)
  .enabled(!!this.nickname.trim())
  .onClick(() => {
    this.save()
  })
```

- 封装修改昵称api 函数
```arkts

/**
 * 更新用户昵称
 * @param nickName
 * @returns
 */
export function updateUserNameApi(nickName: string): Promise<null> {
  return request<null, UpdateUserParamsModel>({
    url: '/employee/user/updateNickname', method: 'put', params: {
      nickName
    }
  })
}
```

- 执行修改
```arkts
 // 提交
  async save() {
    await updateUserNickName(this.nickName)
    getContext().eventHub.emit("updateUser") // 更新
    promptAction.showToast({ message: '修改昵称成功' })
    router.back()
  }
```

- 回到上一个页面同步修改
> 因为onPageShow只要页面进入就会执行，直接去除原有的aboutToAppear即可，将相同逻辑在onPageShow中执行即可

```arkts


  onPageShow(): void {
    Log.info('页面可见')
    this.getUser()
  }
```
> 此时，在Tabs主页中，我的组件并非Page，不具备事件监听条件，此时可以使用线程通信工具emitter来实现

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1718773793415-fdc2e29b-de56-4892-b110-e891c55defc8.png#averageHue=%23c1f2f1&clientId=u370a3196-cfb1-4&from=paste&height=721&id=u16d1f17f&originHeight=1442&originWidth=666&originalType=binary&ratio=2&rotation=0&showTitle=false&size=308682&status=done&style=none&taskId=u5f98416c-e839-4a53-a8d5-dbb1af1e556&title=&width=333)

- 在修改昵称之后发布事件，在我的页面订阅事件
- 发布事件
```typescript
 // 提交
  async save() {
    await updateUserNickName(this.nickName)
    getContext().eventHub.emit("updateUser") // 更新
    promptAction.showToast({ message: '修改昵称成功' })
    router.back()
  }
```

- 在我的页面订阅事件
```typescript
aboutToAppear(): void {
    this.getUser()
    getContext()
      .eventHub.on("updateUser", () => {
      this.getUser()
    })
  }
```
> 提交代码


# 退出登录
> 退出登录
>     提示确认
>     删除token
>     跳转到登录页
>     清除页面栈

```arkts
  logout() {
    AlertDialog.show(
      {
        title: '提示',
        subtitle: '',
        message: '您确认退出登录吗',
        autoCancel: true,
        alignment: DialogAlignment.Bottom,
        gridCount: 4,
        offset: { dx: 0, dy: -20 },
        primaryButton: {
          value: '取消',
          action: () => {

          }
        },
        secondaryButton: {
          enabled: true,
          defaultFocus: true,
          style: DialogButtonStyle.HIGHLIGHT,
          value: '确定',
          action: async () => {
            // 删除token
            await TokenManager.delUser()
            router.clear() // 清空页面栈
            // 跳转到登录栈
            router.replaceUrl({
              url: RouterConfig.LOGIN_URL
            })
          }
        }
      }
    )
  }

```
> 提交代码


# 设备绑定页面-折叠交互
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719472847240-36e12b0e-e2bd-4f11-89ae-fc7223dac8d2.png#averageHue=%23f2d472&clientId=u2831c890-b5be-4&from=paste&height=721&id=u617691e7&originHeight=721&originWidth=333&originalType=binary&ratio=1&rotation=0&showTitle=false&size=118237&status=done&style=none&taskId=u66251e46-c661-4951-a13a-62c88746f07&title=&width=333)
> 本次课程我们讲解门禁系统和鸿蒙应用的交互
> - 此次课程的门禁是**黑马程序员嵌入式学科 + 鸿蒙学科**共同研发的学习硬件，
> - 调试测试需要**华为真机Next系统 + 黑马的自研门禁硬件**，由于测试条件较为苛刻，所以线上同学无法实现真正的代码测试，更重要的是学习 应用 + 硬件设备的编程思想
> 
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719473103728-35970bb9-ac95-47c1-9c84-17af4bd82afd.png#averageHue=%23828988&clientId=u2831c890-b5be-4&from=paste&height=1707&id=u2011a464&originHeight=1707&originWidth=1280&originalType=binary&ratio=1&rotation=0&showTitle=false&size=257523&status=done&style=none&taskId=ueb6b5c27-f0f5-4883-98eb-f18a300d82a&title=&width=1280)

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719474343342-24e50a98-8a2a-47b0-ab8f-cd432e2afbfb.png#averageHue=%2343c7b8&clientId=u2e5c6d41-bda0-4&from=paste&height=721&id=u825daf8f&originHeight=721&originWidth=335&originalType=binary&ratio=1&rotation=0&showTitle=false&size=52747&status=done&style=none&taskId=uc86f2b83-41f6-432c-913d-97161714766&title=&width=335)
> **当点击查看更多时，要展示所有数据**
> 数据结构如下:

```json
[
  {
    "productId": "test_b6e6b64930ad",
    "productName": "test_084b38130633",
    "deviceList": [
      {
        "iotId": "test_310f44adaae5",
        "nodeId": "test_ca719f485de1",
        "deviceName": "test_9681b00eafa6"
      }
    ]
  }
]
```
> 实际上设备列表是**两层的结构，所以需要两层循环**
> ![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719475140422-84f44039-bd12-4325-be00-aaba52eadac0.png#averageHue=%232d2d2c&clientId=u2e5c6d41-bda0-4&from=paste&height=522&id=u92edf681&originHeight=522&originWidth=1492&originalType=binary&ratio=1&rotation=0&showTitle=false&size=41024&status=done&style=none&taskId=ua059a1cc-ad7f-48e8-8c8e-6cbabc92f4f&title=&width=1492)
> ![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719475156745-036c6db3-9c60-4b08-95ac-d9eff2cbcdaa.png#averageHue=%23302f2f&clientId=u2e5c6d41-bda0-4&from=paste&height=854&id=u9a427596&originHeight=854&originWidth=2004&originalType=binary&ratio=1&rotation=0&showTitle=false&size=141197&status=done&style=none&taskId=u7b1480e1-a2cb-45e9-a6ae-7266c4d676b&title=&width=2004)

:::warning
展开折叠，核心思想就是改变item的isExpand属性，只要能够保证该属性变化，然后驱动响应式更新UI即可
:::

- 实现toggle方法
```typescript
 @State
  refreshNum: number = 0

  // 2. 产品选择和折叠
  // 加载更多和收起
  toggle(item: UnBindProductItem) {
    item.isExpand = !item.isExpand
    this.refreshNum++
  }
```

- 使用数字作为ForEach中key的一部分，强制UI更新
```typescript
 ForEach(this.list, (item: UnBindProductItem) => {
              ListItem() {
                this.productItem(item)
              }
            }, (item: UnBindProductItem) => `${JSON.stringify(item)}_${this.refreshNum}`)
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719476520090-d9be8761-22d5-4213-8a70-64a76be6a66f.png#averageHue=%233fc6b6&clientId=u2e5c6d41-bda0-4&from=paste&height=721&id=u0e26c380&originHeight=721&originWidth=338&originalType=binary&ratio=1&rotation=0&showTitle=false&size=58723&status=done&style=none&taskId=u4eebdb21-ae4a-4a98-abff-c3ced13ed60&title=&width=338)
:::warning
我们要知道鸿蒙只能监听到第一层的数据变化，所以我们巧妙的使用一个随时在变化的key作为ForEach的key，这样只要点击了折叠或者展开，那么key发生了变化，UI就会重新更新，达到我们期望的效果
:::
> 提交代码


# 获取未绑定设备

- 抽提未绑定设备类型
```typescript
//  2. 未绑定设备列表
export interface DeviceItem {
  iotId: string
  nodeId: string
  deviceName: string
  isSelected: boolean
}

export interface UnBindProductItem {
  productId: string
  productName: string
  // 是否展开：false 没有点击查看更多 | true 点击了查看更多
  isExpand: boolean
  deviceList: DeviceItem[]
}
export class DeviceItemModel implements DeviceItem {
  iotId: string = ''
  nodeId: string = ''
  deviceName: string = ''
  isSelected: boolean = false

  constructor(model: DeviceItem) {
    this.iotId = model.iotId
    this.nodeId = model.nodeId
    this.deviceName = model.deviceName
    this.isSelected = model.isSelected
  }
}
export class UnBindProductItemModel implements UnBindProductItem {
  productId: string = ''
  productName: string = ''
  isExpand: boolean = false
  deviceList: DeviceItem[] = []

  constructor(model: UnBindProductItem) {
    this.productId = model.productId
    this.productName = model.productName
    this.isExpand = model.isExpand
    this.deviceList = model.deviceList
  }
}

```
> 注意： 如果只有interface，可以使用i2c 一键转化

- 封装API
```typescript
import { request } from '@hm/basic'
import { UnBindProductItemModel } from '../viewmodel'

/**
 * iot 平台 api
 * 查询未绑定设备产品列表
 * @returns
 */
export function getUnbindDeviceListApi(): Promise<UnBindProductItemModel[]> {
  return request<UnBindProductItemModel[]>({ url: '/employee/iot/deviceTree' })
}
```

- 在api/index.ets中导出
```typescript
export * from './DeviceAPI'
```

- 在bindDevice页面中请求数据
```typescript
// 声明数据
@State
list: UnBindProductItemModel[] = []
```

- 在EcList组件中的load事件中加载数据
```typescript
load = async () => {
    // 请求数据
    this.list = await getUnbindDeviceListApi()
  }
```

- 替换所有的interface类型为class类型

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719477780136-99c3a087-7de1-44af-8cca-61d92d64075c.png#averageHue=%232e2d2c&clientId=u2e5c6d41-bda0-4&from=paste&height=674&id=ua41bc5b1&originHeight=674&originWidth=1986&originalType=binary&ratio=1&rotation=0&showTitle=false&size=93542&status=done&style=none&taskId=u74b71080-55f1-48ad-bae1-2edb281af21&title=&width=1986)
> 这里可能又会有同学有这种疑问？
> interface和class到底什么时候用？区别是什么？
> 回答： **原则上都没问题，但是interface只能用作类型，class不但可以用作类型，还可以用来构造，当成实际的对象来使用，另外当使用Observed的时候必须使用class, 除此之外，没什么区别，但是官方更建议我们使用class，因为class可以进行构造，更方便赋值，至于用interface和class取决于每个公司的开发规范**

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719477742162-6bdf1108-1495-4ea2-a3a9-798f7ad08438.png#averageHue=%234bc4b6&clientId=u2e5c6d41-bda0-4&from=paste&height=721&id=ub649099d&originHeight=721&originWidth=333&originalType=binary&ratio=1&rotation=0&showTitle=false&size=45881&status=done&style=none&taskId=u19a18947-ccac-455c-a870-da224163280&title=&width=333)
> 提交代码

# 选择设备实现
> - 选择某个设备有UI效果
> - 只有选择了设备才可以进行下一步确定
> - 要记录选择的哪个分类的哪个设备-分类和设备都有被选中选项
> - 同时只能选择一个设备


:::success
实现方式和之前的展开折叠一样，赋值某个属性即可。只不过要考虑用户有取消选择的需求
:::
```arkts
// 选择某个产品下设备
  /**
   *
   * @param device 当前点击产品下的某个设备
   */
  selectDevice(item: UnBindProductItemModel, device: DeviceItemModel) {
    this.list.forEach(prod => {
      prod.deviceList.forEach(dev => {
        if (dev.isSelected) {
          dev.isSelected = false
        }
      })
    })
    device.isSelected = !device.isSelected
    this.refreshNum++
  }
```

- 定义当前的状态记录当前选择设备
```typescript
  @State
  currentProduct: UnBindProductItemModel = new UnBindProductItemModel({} as UnBindProductItem)
  @State
  currentSelect: DeviceItemModel = new DeviceItemModel({} as DeviceItem)
```

- 选择时记录
```typescript
// 选择某个产品下设备
  /**
   *
   * @param device 当前点击产品下的某个设备
   */
  selectDevice(item: UnBindProductItemModel, device: DeviceItemModel) {
    this.list.forEach(prod => {
      prod.deviceList.forEach(dev => {
        if (dev.isSelected) {
          dev.isSelected = false
        }
      })
    })
    device.isSelected = !device.isSelected
    this.currentSelect = device // 记录当前选择的设备
    this.currentProduct = item
    this.refreshNum++
  }
```

- 控制确认按钮是否可点击
```arkts
Button('确认')
  .width(312)
  .backgroundColor(ResManager.EC_MAIN_COLOR)
  .enabled(!!this.currentSelect.iotId) // 判断是否有lotId来决定按钮是否可点击
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719479230394-9ca1dca8-d2b7-43c2-9329-df1daa1bfe0d.png#averageHue=%2343c4b5&clientId=u2e5c6d41-bda0-4&from=paste&height=721&id=u79c65a29&originHeight=721&originWidth=335&originalType=binary&ratio=1&rotation=0&showTitle=false&size=48194&status=done&style=none&taskId=u2df336ef-ffd9-40cb-a5e0-8665555747b&title=&width=335)
> 提交代码


# 设备英文映射到中文
> 由于设备通信的要求，在给设备命名时需要考虑中文的判断代码的合理性，所以后台实际上以英文命名，但是在客户端展示用户需要简单的中文来识别，所以我们准备一个字典库进行匹配

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719479832714-6ffd59e9-6b7c-45d9-aa08-3ee7627844ae.png#averageHue=%2347c4b6&clientId=u2e5c6d41-bda0-4&from=paste&height=721&id=udedb5f6b&originHeight=721&originWidth=333&originalType=binary&ratio=1&rotation=0&showTitle=false&size=45902&status=done&style=none&taskId=u7f958f86-9198-499e-bc8a-08f796dea87&title=&width=333)
```arkts
import { ProductNameList } from '../constants'

// 根据英文获取中文的方法

export function getDeviceMap(name: string) {
  return ProductNameList.find(item => item.en === name) // 返回的是个对象
}
```

- 在utils/index.ets中导出
```typescript
export * from './Device'
```

- 将设备中的英文替换成中文

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1720186702550-70fa33da-7e29-46d6-a1a0-150acbbb5533.png#averageHue=%23323131&clientId=uac7b203b-1872-4&from=paste&height=388&id=u27e3b2ce&originHeight=388&originWidth=712&originalType=binary&ratio=1&rotation=0&showTitle=false&size=57435&status=done&style=none&taskId=u5e76013b-6e3b-4b8b-98f3-66789ef2575&title=&width=712)
```typescript
  @Builder
  productItem(item: UnBindProductItemModel) {
    Column({ space: 18 }) {
      // 1. 产品分类
      Row() {
        Text(getDeviceMap(item.productName)?.cn || '--')
          .fontColor(ResManager.EC_MID_19)
          .fontSize(ResManager.EC_MODULE_TITLE_FS)
          .fontWeight(500)
        // 点击查看更多后显示
        if (item.isExpand) {
          Text() {
            Span('收起')
            ImageSpan($r('app.media.ic_arrow_up'))
              .width(12.8)
              .height(5.6)
              .verticalAlign(ImageSpanAlignment.CENTER)
              .margin({ bottom: 2 })
          }
          .fontColor(ResManager.EC_MAIN_COLOR)
          .fontSize(ResManager.EC_SMALL_FS)
          .onClick(() => {
            this.toggle(item)
          })
        }
      }
      .width('100%')
      .justifyContent(FlexAlign.SpaceBetween)

      //  2. 设备列表
      GridRow({ columns: 2, gutter: 10 }) {
        // 数据超出 6 同时没有点击查看更多
        if (item.deviceList?.length! > 6 && !item.isExpand) {
          ForEach(item.deviceList?.slice(0, 6), (device: DeviceItemModel, i: number) => {
            GridCol() {
              // Column() {
              if (i < 5) {
                Column() {
                  // 选择设备
                  Text(device.deviceName)
                    .fontColor(device.isSelected ? '#fff' : ResManager.EC_MID_19)
                    .fontSize(ResManager.EC_SMALL_FS)
                }
                .width('100%')
                .padding({
                  left: 15,
                  right: 15,
                  top: 10,
                  bottom: 10
                })
                .borderRadius(ResManager.EC_NORMAL_RD)
                .backgroundColor(device.isSelected && i < 5 ? ResManager.EC_MAIN_COLOR : '#fff')
                .onClick(() => {
                  this.selectDevice(device)
                })
              } else {
                Column() {
                  // 数据超出 6条 显示
                  Text() {
                    Span('查看更多')
                    ImageSpan($r('app.media.ic_arrow_down'))
                      .width(12.8)
                      .height(5.6)
                      .verticalAlign(ImageSpanAlignment.CENTER)
                      .margin({ bottom: 2 })
                  }
                  .fontColor(ResManager.EC_MAIN_COLOR)
                  .fontSize(ResManager.EC_SMALL_FS)
                }
                .width('100%')
                .padding({
                  left: 15,
                  right: 15,
                  top: 10,
                  bottom: 10
                })
                .borderRadius(ResManager.EC_NORMAL_RD)
                .backgroundColor(device.isSelected && i < 5 ? ResManager.EC_MAIN_COLOR : '#fff')
                .onClick(() => {
                  this.toggle(item)
                })
              }
            }
          }
          )
        } else {
          ForEach(item.deviceList, (device: DeviceItemModel) => {
            GridCol() {
              Column() {
                Text(device.deviceName)
                  .fontColor(device.isSelected ? '#fff' : ResManager.EC_MID_19)
                  .fontSize(ResManager.EC_SMALL_FS)
              }
              .width('100%')
              .padding({
                left: 15,
                right: 15,
                top: 10,
                bottom: 10
              })
              .borderRadius(ResManager.EC_NORMAL_RD)
              .backgroundColor(device.isSelected ? ResManager.EC_MAIN_COLOR : '#fff')
              .onClick(() => {
                this.selectDevice(device)
              })
            }
          }
          )
        }

      }

    }
    .width('100%')
  }
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719481213820-0c77ebf6-6d2a-4255-9c3f-cff081657c6e.png#averageHue=%23a3e1dc&clientId=u2e5c6d41-bda0-4&from=paste&height=721&id=u2b534d5d&originHeight=721&originWidth=332&originalType=binary&ratio=1&rotation=0&showTitle=false&size=41817&status=done&style=none&taskId=u2cb44c37-a3bf-4f91-bc0d-9eb40b5e1f6&title=&width=332)

> 提交代码

# 检查蓝牙权限
> 需求分析：使用门禁开门要完成以下几件事
> - 检测是否有蓝牙权限-应用的事情
> - 检测是否打开蓝牙
> - 蓝牙打开-扫描可用设备
> - 通过蓝牙连接设备-获取读写数据权限
> - 通过蓝牙读写权限给门禁配置wifi账户
> - 通过蓝牙连接发送开门指令


:::success

- 检查蓝牙权限

类似于前面同学做过的微信案例，我们检查麦克风权限的流程
为了体现封装的特性，我们单独封装一个蓝牙的检查授权的管理器，因为只在设备中，可以直接在device的hsp中创建，当然也可以放在basic模块中
:::

- 配置蓝牙权限
```typescript
{
    "name": "ohos.permission.ACCESS_BLUETOOTH",
    "reason": "$string:app_name",
    "usedScene": {
      "abilities": [ "EntryAbility"],
      "when":"inuse"
    }
  }
```

- 新建蓝牙权限管理器
```arkts
class BlueManager {
  // 检查蓝牙授权
  checkBlue() {
  }
  // 检查蓝牙是否打开
  checkBlueOpen() {

  }
}

export const blueManager = new BlueManager()
```

- 在utils/index.ets中导出
```typescript
export * from './BlueManager'
```

- 实现检查蓝牙授权方法

[文档中心](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-bundlemanager-0000001774280558-V5#ZH-CN_TOPIC_0000001881257737__bundlemanagergetbundleinfoforself)
```typescript
// 检查蓝牙授权
  async checkBlue() {
    try {
      // 1. 获取应用信息
      const bundleInfo =
        await bundleManager.getBundleInfoForSelf(bundleManager.BundleFlag.GET_BUNDLE_INFO_WITH_APPLICATION)
      // 2. 检查蓝牙权限
      let atManager: abilityAccessCtrl.AtManager = abilityAccessCtrl.createAtManager();
      let permissionName: Permissions = 'ohos.permission.ACCESS_BLUETOOTH';
      let data: abilityAccessCtrl.GrantStatus =
        atManager.checkAccessTokenSync(bundleInfo.appInfo.accessTokenId, permissionName);
      Log.info('蓝牙权限：' + data)
    } catch (err) {
      let message = (err as BusinessError).message;
      Log.error(message)
    }
  }
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719482976421-6e769b45-4234-4867-850c-8b1e678a30bb.png#averageHue=%23343434&clientId=u2e5c6d41-bda0-4&from=paste&height=300&id=u9f6fb059&originHeight=300&originWidth=2142&originalType=binary&ratio=1&rotation=0&showTitle=false&size=39430&status=done&style=none&taskId=u79f85882-a8a2-4bc8-9bdf-799d0e5e2f0&title=&width=2142)

- 当没有蓝牙权限时，申请蓝牙权限
```typescript
  // 检查蓝牙权限-应用有没有蓝牙权限
  async checkBlue() {
    // 权限的检查
    const bundleInfo = bundleManager.getBundleInfoForSelfSync(bundleManager.BundleFlag.GET_BUNDLE_INFO_WITH_APPLICATION)
    let atManager = abilityAccessCtrl.createAtManager()
    const status =
      atManager.checkAccessTokenSync(bundleInfo.appInfo.accessTokenId, "ohos.permission.ACCESS_BLUETOOTH") // 检查是否授权
    if (status === abilityAccessCtrl.GrantStatus.PERMISSION_DENIED) {
      // 要去申请蓝牙权限
      const result = await atManager.requestPermissionsFromUser(getContext(), ["ohos.permission.ACCESS_BLUETOOTH"])
      // 能否确定授权吗？？？？
      // AlertDialog.show({ message: result.authResults[0] + "" })
      if (result.authResults[0] === 0) {
        // 通过了

      } else {
        // 如果不等于0 那就是点击了禁止
        // 拉起应用系统设置页面
        let context = getContext(this) as common.UIAbilityContext;
        context.startAbility({
          bundleName: 'com.huawei.hmos.settings',
          abilityName: 'com.huawei.hmos.settings.MainAbility',
          uri: 'application_info_entry',
          parameters: {
            pushParams: 'com.hm.yk.yl' // 应用包名
          }
        });

      } // 0 就是通过 -1 是不通过

    }
  }
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719487819159-2af570c1-30f3-4b5b-9f9f-dea882c179d4.png#averageHue=%23329f92&clientId=u2e5c6d41-bda0-4&from=paste&height=721&id=u0c28611b&originHeight=721&originWidth=334&originalType=binary&ratio=1&rotation=0&showTitle=false&size=51193&status=done&style=none&taskId=u79a4af59-0aff-4aec-a79c-6d53bd1d04f&title=&width=334)
> 提交代码

# 检查蓝牙功能开关
> 上一步只是检查我们的应用是否有控制蓝牙的权限，这一步我们检查我们的手机系统是否开了蓝牙开关
> ![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719487923242-be07fa14-c7dc-4f28-939f-1006415089a7.png#averageHue=%23aed1db&clientId=u2e5c6d41-bda0-4&from=paste&height=721&id=uf61cfee2&originHeight=721&originWidth=333&originalType=binary&ratio=1&rotation=0&showTitle=false&size=235419&status=done&style=none&taskId=ua98ef30d-f285-4a3a-a0e3-74e56b823e2&title=&width=333)
> **坑点：模拟器无法检测蓝牙开关**

- 封装蓝牙是否打开方法
```arkts
  // 检查蓝牙是否打开
  checkBlueOpen() {
    return access.getState() === access.BluetoothState.STATE_ON
  }
```

- 授权后检查蓝牙开关是否打开，如果打开正常进行下一步，否则抛出异常
```typescript
import abilityAccessCtrl from '@ohos.abilityAccessCtrl'
import { bundleManager, common } from '@kit.AbilityKit'
import { promptAction } from '@kit.ArkUI'
import { access } from '@kit.ConnectivityKit'

class BlueManager {
  // 检查蓝牙权限-应用有没有蓝牙权限
  async checkBlue() {
    try {
      // 权限的检查
      const bundleInfo =
        bundleManager.getBundleInfoForSelfSync(bundleManager.BundleFlag.GET_BUNDLE_INFO_WITH_APPLICATION)
      let atManager = abilityAccessCtrl.createAtManager()
      const status =
        atManager.checkAccessTokenSync(bundleInfo.appInfo.accessTokenId, "ohos.permission.ACCESS_BLUETOOTH") // 检查是否授权
      if (status === abilityAccessCtrl.GrantStatus.PERMISSION_DENIED) {
        // 要去申请蓝牙权限
        const result = await atManager.requestPermissionsFromUser(getContext(), ["ohos.permission.ACCESS_BLUETOOTH"])
        // 能否确定授权吗？？？？
        // AlertDialog.show({ message: result.authResults[0] + "" })
        if (result.authResults[0] === 0) {
          // 通过了
          if (!this.checkBlueOpen()) {
            // 应该打开蓝牙开关 检测开关是关着的
            return Promise.reject(new Error("请首先打开蓝牙开关"))
          }
          return true // 表示检查权限成功

        } else {
          // 如果不等于0 那就是点击了禁止
          // 拉起应用系统设置页面
          let context = getContext(this) as common.UIAbilityContext;
          context.startAbility({
            bundleName: 'com.huawei.hmos.settings',
            abilityName: 'com.huawei.hmos.settings.MainAbility',
            uri: 'application_info_entry',
            parameters: {
              pushParams: 'com.hm.yk.yl' // 应用包名
            }
          });
          return false // 表示授权失败 跳到了设置页面
        } // 0 就是通过 -1 是不通过

      }
      // 已经有蓝牙权限
      if (!this.checkBlueOpen()) {
        // 检测开关是关着的
        return Promise.reject(new Error("请首先打开蓝牙开关"))
      }
      return true // 表示授权成功
    } catch (error) {
      return false
    }
  }

  // 检查系统的蓝牙是否打开 true/打开 false/关闭
  checkBlueOpen() {
    return access.getState() === access.BluetoothState.STATE_ON
  }
}

export const blueManager = new BlueManager()
```

- 确定按钮处理跳转
```typescript
async bindDevice() {
    try {
      // 这里面检查蓝牙
      const isOK = await blueManager.checkBlue() // 检查蓝牙是否授权
      promptAction.showToast({ message: isOK ? '授权成功' : "授权失败" })
      if (isOK) {
        // 进行下一步的通信 扫描蓝牙
        router.pushUrl({
          url: RouterConfig.DEVICE_NEAR_URL
        })
      }
    } catch (error) {
      promptAction.showToast({ message: error.message })
    }
  }
```

- 绑定确定按钮点击事件
```typescript
 Button('确认')
            .width(312)
            .backgroundColor(ResManager.EC_MAIN_COLOR)
            .onClick(() => {
              this.bindDevice() // 绑定设备
            })
            .enabled(!!this.currentSelect.iotId)
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719491626956-c140842a-5a0a-4130-96df-86e5d6940d9c.png?x-oss-process=image/format,png#averageHue=%23fefefa&clientId=u2e5c6d41-bda0-4&from=paste&height=825&id=ud0b15756&originHeight=2720&originWidth=1260&originalType=binary&ratio=1&rotation=0&showTitle=false&size=13709850&status=done&style=none&taskId=u42d5ccc4-beb8-4d8f-ba42-60a2c77dbaf&title=&width=382)

> 提交代码


# 记录当前设备的lot信息
> 我们需要在连接页面使用之前选择设备的lot信息，如lotId-设备id和nodeId-设备标识
> **但是如果通过页面传递，下次再使用还得一次次的传递，比较麻烦。**
> **可以通过全局状态共享，也可以使用单例控制。全局状态的话因为只属于设备模块，所以我们还是采用单例模式**

- 定义存储信息的类型
```arkts
export interface LotParams {
  // 设备 Id
  iotId: string | null
  // 设备标识
  nodeId: string | null
  // 老人 Id
  elderId: string | null
  // 测量时间：凌晨0-睡前7
  dateType: number | null
}

export class LotParamsModel implements LotParams {
  iotId: string | null = null
  nodeId: string | null = null
  elderId: string | null = null
  dateType: number | null = null

  constructor(model: LotParams) {
    this.iotId = model.iotId
    this.nodeId = model.nodeId
    this.elderId = model.elderId
    this.dateType = model.dateType
  }
}

// 导出当前单例
export const currentLot = new LotParamsModel({} as LotParams)
```

- 在viewmodels/index.ets中导出
```typescript
export * from './LotModel'
```

- 在跳转连接蓝牙之前进行赋值
```typescript
async bindDevice() {
    try {
      // 这里面检查蓝牙
      const isOK = await blueManager.checkBlue() // 检查蓝牙是否授权
      promptAction.showToast({ message: isOK ? '授权成功' : "授权失败" })
      if (isOK) {
        currentLot.nodeId = this.currentDevice.nodeId // 记录nodeId
        currentLot.iotId = this.currentDevice.iotId // 记录当前的iotId
        // 进行下一步的通信 扫描蓝牙
        router.pushUrl({
          url: RouterConfig.DEVICE_NEAR_URL
        })
      }
    } catch (error) {
      promptAction.showToast({ message: error.message })
    }
  }
```
> 提交代码

# 封装蓝牙管理器
> **封装设备蓝牙操作：**
> 1. 设备扫描
> 2. 设备连接
> 3. 设备通信

- 蓝牙开发概述
> 官方文档： [https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/bluetooth-overview-V5](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/bluetooth-overview-V5)
> 
> ## 模块介绍
> - a2dp模块（高级音频分发配置文件）：A2DP是Advanced Audio Distribution Profile的缩写，即高级音频分发配置文件。它是一种蓝牙协议，允许无线传输高品质音频流，例如音乐或语音通话，同时支持双向通信，因此可以用于耳机、扬声器、汽车音响等设备。详情请参考[@ohos.bluetooth.a2dp API参考](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-bluetooth-a2dp-V5)。
> - ble模块（低功耗蓝牙）：BLE是Bluetooth Low Energy的缩写，意为“低功耗蓝牙”。它是一种能够在低功耗情况下进行通信的蓝牙技术，与传统蓝牙相比，BLE的功耗更低，适用于需要长时间运行的低功耗设备，如智能手表、健康监测设备、智能家居等。详情请参考[@ohos.bluetooth.ble API参考](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-bluetooth-ble-V5)。
> - hfp模块（免提模式）：HFP模块是指蓝牙耳机或车载蓝牙设备中的Hands-Free Profile，即“免提模式”。HFP允许用户通过蓝牙连接手机或其他蓝牙设备，实现免提通话和语音控制等功能。详情请参考[@ohos.bluetooth.hfp API参考](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-bluetooth-hfp-V5)。
> - hid模块（人机接口设备）：HID是Human Interface Device的缩写，即人机接口设备。在蓝牙中，HID模块是一种允许用户通过蓝牙连接键盘、鼠标、游戏手柄等人机接口设备的模块。用户可以通过HID模块将这些设备连接到蓝牙主机上，实现无线控制和输入。详情请参考[@ohos.bluetooth.hid API参考](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-bluetooth-hid-V5)。
> - pan模块（个人区域网络）：PAN（Personal Area Network）是个人区域网络的缩写，是一种无线通信技术，用于将设备连接到一个小范围的网络中。例如可以将手机、电脑、打印机等设备连接到一个PAN网络中，实现文件的共享和打印。详情请参考[@ohos.bluetooth.pan API参考](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-bluetooth-pan-V5)。
> - gatt模块（通用属性）：GATT是指蓝牙技术中的通用属性（Generic Attribute），它是一种用于在蓝牙低功耗设备之间传输数据的协议。GATT协议定义了一套通用的属性和服务框架，用于描述蓝牙设备之间的通信，同时蓝牙设备可以向其他设备提供服务，也可以从其他设备获取服务。详情请参考[@ohos.bluetooth.ble API参考](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-bluetooth-ble-V5)。
> - spp模块（串口协议）：SPP是Serial Port Profile（串口协议）的缩写，是一种蓝牙协议，用于在蓝牙设备之间建立串行通信连接。通过SPP，蓝牙设备可以像使用串口一样进行数据传输，例如传输文件、文本等。详情请参考[@ohos.bluetooth.socket API参考](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-bluetooth-socket-V5)。

- 首选保证我们在主hap包中开启了蓝牙的控制权限(前面已经设置过)

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719544959777-f41d3320-3d1f-4ea3-9bf7-ff457d6c7474.png#averageHue=%232c2c2c&clientId=u2e5c6d41-bda0-4&from=paste&height=640&id=uc1d33274&originHeight=640&originWidth=1440&originalType=binary&ratio=1&rotation=0&showTitle=false&size=50796&status=done&style=none&taskId=u188654da-1cf6-458c-ae6f-3d5fe4cc3fa&title=&width=1440)

- 在之前的BlueManager中添加一个扫描设备的方法
```typescript
// 扫描设备
  scanDevice() {
    
  }
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719545063199-c7f97803-0c90-47f0-83c7-ba2edf55f50e.png#averageHue=%232f2f2e&clientId=u2e5c6d41-bda0-4&from=paste&height=1446&id=u961b0cb9&originHeight=1446&originWidth=1794&originalType=binary&ratio=1&rotation=0&showTitle=false&size=187338&status=done&style=none&taskId=ue73b675a-6fca-46c1-8155-3c8e8f2973a&title=&width=1794)
> 我们应用和门禁通信采用的低功耗蓝牙，所以需要使用**ble**的功能模块
> 文档地址： [https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-bluetooth-ble-V5#blestartblescan](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-bluetooth-ble-V5#blestartblescan)

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719545215128-8d1456fc-b987-4074-9772-eec2df7ffee7.png#averageHue=%23f7f7f7&clientId=u2e5c6d41-bda0-4&from=paste&height=540&id=u73eeafd8&originHeight=540&originWidth=1000&originalType=binary&ratio=1&rotation=0&showTitle=false&size=82205&status=done&style=none&taskId=ud729e289-192b-4cce-8339-8e251e40a66&title=&width=1000)
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719545316500-2b609f6c-5e6f-4251-89a6-d6b9ff7abb4c.png#averageHue=%23f7f7f7&clientId=u2e5c6d41-bda0-4&from=paste&height=816&id=uac7f40ed&originHeight=816&originWidth=997&originalType=binary&ratio=1&rotation=0&showTitle=false&size=123061&status=done&style=none&taskId=ubb59792d-0dc8-49f4-8c4b-71a97922b45&title=&width=997)
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719545298729-c916e1c8-eae0-4a9f-8318-20b2f5b832a9.png#averageHue=%23f8f8f8&clientId=u2e5c6d41-bda0-4&from=paste&height=608&id=ue1fa44ec&originHeight=608&originWidth=978&originalType=binary&ratio=1&rotation=0&showTitle=false&size=84944&status=done&style=none&taskId=u8b079fd9-edad-4122-ac2f-072c50b6111&title=&width=978)
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719545489634-4fa0d341-d5f5-493b-9d1e-bb9041dbf028.png#averageHue=%23eef0f1&clientId=u2e5c6d41-bda0-4&from=paste&height=594&id=uca507d22&originHeight=594&originWidth=991&originalType=binary&ratio=1&rotation=0&showTitle=false&size=95990&status=done&style=none&taskId=u35a64cee-7c31-421c-8c80-9e5a2c7610f&title=&width=991)

- 实现扫描蓝牙方法
```typescript
// 扫描设备
  scanDevice() {
    try {
      ble.on("BLEDeviceFind", (list: ble.ScanResult[]) => {
        Log.info("扫描到当前蓝牙列表：" + JSON.stringify(list))
      });
      let scanOptions: ble.ScanOptions = {
        interval: 500,
        dutyMode: ble.ScanDuty.SCAN_MODE_LOW_POWER,
        matchMode: ble.MatchMode.MATCH_MODE_AGGRESSIVE
      }
      ble.startBLEScan(null, scanOptions);
    } catch (err) {
      Log.error('errCode: ' + (err as BusinessError).code + ', errMessage: ' + (err as BusinessError).message);
    }
  }
```

> 扫描到设备后将扫描后的设备通过回调函数进行回传，并对数据进行重复处理，回传之后，去掉扫描监听

- 代码
```typescript
 timer: number = -1 // 定义定时器

   // 扫描蓝牙
  scanDevice(callback: (res: ble.ScanResult[]) => void) {
    try {
      clearTimeout(this.timer) // 清除上一次的 重新开始
      // 当找到蓝牙时会触发
      const resList: ble.ScanResult[] = [] // 汇总所有的发现蓝牙
      ble.on("BLEDeviceFind", (list: ble.ScanResult[]) => {
        Log.info("扫描到的蓝牙列表：" + JSON.stringify(list))
        // [1,2,3] [2]
        resList.push(...list.filter(dv =>!resList.some(item => item.deviceId === dv.deviceId)))
        // 告诉页面
        callback(resList) // 回传汇总的数据
      });
      let scanOptions: ble.ScanOptions = {
        interval: 500,
        dutyMode: ble.ScanDuty.SCAN_MODE_LOW_POWER,
        matchMode: ble.MatchMode.MATCH_MODE_AGGRESSIVE
      }
      ble.startBLEScan(null, scanOptions);
      this.timer = setTimeout(() => {
        ble.stopBLEScan() // 停止扫描
        ble.off("BLEDeviceFind") // 停止监听该事件
      }, 5000)
    } catch (err) {
      Log.error('errCode: ' + (err as BusinessError).code + ', errMessage: ' + (err as BusinessError).message);
    }
  }
```

- 在扫描页面进行处理
```typescript
 @State
  list: ble.ScanResult[] = []

  aboutToAppear(): void {
    this.reset()
  }

  // 3. 搜索方法
  search() {
    blueManager.scanDevice((res) => {
      if (res.length) {
        this.hasDevice = true // 发现设备
        this.list = res
      }
    })
  }
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719676546898-371376da-ef33-40bc-b3b5-055478410cba.png#averageHue=%23303030&clientId=u1139f301-74b3-4&from=paste&height=446&id=u4323467d&originHeight=1166&originWidth=1536&originalType=binary&ratio=1&rotation=0&showTitle=false&size=165718&status=done&style=none&taskId=udad03607-00b0-4484-9a3e-3d499aea943&title=&width=587)
![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1714466709349-709cf786-9230-4cb8-af7f-33f55add0431.png#averageHue=%23aeded8&clientId=u04633c6a-70bb-4&from=paste&height=706&id=qcJ0N&originHeight=724&originWidth=370&originalType=binary&ratio=2&rotation=0&showTitle=false&size=43027&status=done&style=none&taskId=u2a462c7b-8ab0-480f-8c79-02f10e84ca1&title=&width=361)
> 提交代码


# 搜索动画
> 当没有搜索结果时，显示动画

- 当开始搜索时，开启动画
```typescript

  // 3. 搜索方法
  // 3. 搜索方法
  search() {
    this.run() // 开始动画
    blueManager.scanDevice((list) => {
      this.list = list
    })
    setTimeout(() => {
      if (this.list.length) {
        this.hasDevice = true
      }
      clearInterval(this.timer) // 停止动画
    }, 5000)
  }

run() {
    this.timer = setInterval(() => {
      this.angle += this.step
    }, 1000 / 60)
  }
```

- 当发现设备后，关掉动画
```arkts
@State
hasDevice: boolean = false

```

- 实现动画效果
```typescript
Image(ResManager.IC_FJSB_SS)
              .width(180)
              .aspectRatio(1)
              .rotate({
                angle: this.angle
              })
              .animation({
                duration: 200
              })
```
> 提交代码


# 筛选特定黑马门禁设备
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719677685185-23d21f5a-28e5-4a75-8724-d36715b7fa4d.png#averageHue=%23fdfefb&clientId=u1139f301-74b3-4&from=paste&height=1576&id=ud7c8c21b&originHeight=1576&originWidth=766&originalType=binary&ratio=1&rotation=0&showTitle=false&size=252071&status=done&style=none&taskId=uc0c2a04e-5970-452c-a93b-c89bc4605d4&title=&width=766)

> 如上图所以，ble扫描出太多无用设备，而黑马门禁有一个特定的前缀JL-ITHEIMA，所以我们直接在数据中筛选一下即可

- 筛选代码
```typescript
  search() {
    this.run() // 开始动画
    blueManager.scanDevice((list) => {
      this.list = list.filter(item => item.deviceName.startsWith("JL-ITHEIMA"))
    })
    setTimeout(() => {
      if (this.list.length) {
        this.hasDevice = true
      }
      clearInterval(this.timer) // 停止动画
    }, 5000)
  }
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719677979319-57fdfb08-77bc-483d-9034-c614f2f55a28.png#averageHue=%23f5f5f7&clientId=u1139f301-74b3-4&from=paste&height=986&id=ucc0494db&originHeight=1552&originWidth=740&originalType=binary&ratio=1&rotation=0&showTitle=false&size=68209&status=done&style=none&taskId=u4df0a797-e567-4758-b3ed-aacb22d67c6&title=&width=470)
> 提交代码


# 连接黑马门禁设备

- 点击特定的黑马门禁设备
> ble设备连接文档： [https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-bluetooth-ble-V5#gattclientdevice](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-bluetooth-ble-V5#gattclientdevice)
> 需求：连接门禁设备进行开门操作
> **连接流程：**
> 1. 扫描发现设备
> 2. 建立连接
> 3. **获取蓝牙服务（服务即蓝牙设备提供的功能）=> 通信**
>    1. 通知特征值对象（获取蓝牙设备发送数据）
>    2. 写入特征值对象（给蓝牙设备发送数据）

- 在blueManager中声明一个方法
```typescript
  // 连接蓝牙设备
  connectBlue(deviceId: string) {
    
  }
```

> 蓝牙服务
> 在连接蓝牙设备时，查找**蓝牙服务**是一项重要的步骤。蓝牙服务是蓝牙设备上提供的功能或特性的抽象表示，它们定义了设备可以提供的各种功能，比如数据传输、音频播放、传感器数据等。每个**蓝牙服务都包含一个或多个特征**（Characteristics），这些特征描述了服务所提供的具体功能或属性。
> 查找蓝牙服务的主要作用包括：
> 1. 识别设备功能： 通过查找蓝牙服务，可以了解到连接的蓝牙设备所提供的功能和特性。这有助于应用程序确定如何与设备进行交互，并选择正确的服务来执行所需的操作。
> 2. 建立通信通道： 一旦找到了需要的蓝牙服务，应用程序可以与设备建立通信通道，以便发送和接收数据。通常，应用程序需要与设备的特定服务进行通信，才能执行相应的操作。
> 3. 配置和控制设备： 通过蓝牙服务，应用程序可以向设备发送命令或配置参数，以控制设备的行为或进行设置。例如，控制蓝牙音箱的音量、切换播放模式等。
> 4. 获取设备信息： 蓝牙服务通常还包含有关设备的一些信息，如设备名称、型号、制造商信息等。应用程序可以通过服务获取这些信息，以便在用户界面中显示或用于其他目的。
> 
总之，**查找蓝牙服务是连接蓝牙设备并与之通信的重要步骤**，它允许应用程序识别设备功能并与之交互，从而实现各种有用的功能和应用场景。


![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719678833672-c4cefbbe-9c2b-42e6-bc71-6e34185332f9.png#averageHue=%23f5f5f5&clientId=u1139f301-74b3-4&from=paste&height=532&id=u0db63276&originHeight=1064&originWidth=1392&originalType=binary&ratio=1&rotation=0&showTitle=false&size=125070&status=done&style=none&taskId=u9187e4c0-d225-42d3-928d-7cfac220e2a&title=&width=696)
> 二进制数据操作
> - Uint8Array函数
> 
在 JavaScript 中，Uint8Array 是一种特殊的类型化数组（TypedArray），用于表示 8 位无符号整数值的数组。它提供了一种有效的方式来处理二进制数据，例如图像数据、音频数据等。
> 重要概念和作用：
> 1. 数据类型化：Uint8Array 是一种类型化数组，这意味着它包含的数据是一种特定的类型，即 8 位无符号整数。这使得在处理二进制数据时更加高效。
> 2. 内存分配： 与普通数组不同，Uint8Array 使用底层的内存分配机制，可以在创建时分配一块固定大小的内存。这使得它更适合处理大量二进制数据，而无需频繁的内存分配和释放。
> 3. 索引访问： 与普通数组类似，你可以使用索引来访问 Uint8Array 中的元素。例如，myUint8Array[0] 将返回数组中的第一个元素。
> 4. 二进制数据处理：Uint8Array 常用于处理二进制数据，如图像、音频、视频等。你可以通过 Uint8Array 来读取、修改、传输和存储二进制数据。
> 5. 与 ArrayBuffer 的关系：Uint8Array 的数据通常存储在 ArrayBuffer 对象中。ArrayBuffer 提供了一种用于在 JavaScript 中表示通用的、固定长度的原始二进制数据缓冲区的方式。Uint8Array 可以视图（view）这个缓冲区，并提供了一种更方便的方式来操作其中的数据。
> 
总之，**Uint8Array 提供了一种高效处理二进制数据的方式**，特别是在需要处理大量数据时。它的使用场景涵盖了诸如图像处理、音频处理、网络通信等各种领域。
> 
> - 进制作用和场景
> 1. **二进制（Binary）**：
>    - 是计算机内部最基础的进制方式。
>    - 用“0”和“1”两种状态表示电路的开和关。
>    - 广泛应用于数字处理、网络通信、加密解密等。
> 2. **八进制（Octal）**：
>    - 早期在一些计算机系统中使用，如PDP-8等。
>    - 由于每个八进制数字代表三个二进制数字，因此在某些情况下可以简化表示。
> 3. **十进制（Decimal）**：
>    - 日常生活中最常用的进制方式。
>    - 商业世界中的货币、价格等常用十进制表示。
> 4. **十六进制（Hexadecimal）**：
>    - 在网络和通信技术中广泛使用，如IP地址。
>    - 在Web颜色编码、图像处理等领域也有应用。
>    - 十六进制提供了一种直观且紧凑的表示方式，方便处理底层的二进制数据。
> 
这些不同的进制在计算机科学和技术领域扮演着重要的角色，根据应用场景的不同，选择适合的进制可以提高处理效率和准确性。


> 说明：**重点了解蓝牙设备的连接和通信流程**
> **通信格式：JSON**
> 约定：**门禁设备服务serviceUuid以：0000AE30 开头**（16）
> 1. 发现门禁设备服务=》dev.getServices()
> 2. 根据约定查找服务=》serviceUuid
> 3. 获取读写特征值对象=》characteristics
>    1. 写入特征值以：**0000AE10 开头**
>    2. 通知特征值以：**0000AE04 开头**

- 在connect方法中实现如下流程
> - 创建gatt通信实例
> - 调用连接方法connect
> - 监听连接状态确认连接成功

```typescript
currentClient: ble.GattClientDevice | null = null

  // 连接蓝牙
  connectBlue(deviceId: string, callback: () => void) {
    if (deviceId) {
      // 创建gatt的实例对象
      this.currentClient = ble.createGattClientDevice(deviceId) // 和蓝牙通信的对象的实例
      this.currentClient.on("BLEConnectionStateChange", (res) => {
        if (res.state === constant.ProfileConnectionState.STATE_CONNECTED) {
          callback() // 连接成功
          promptAction.showToast({ message: '蓝牙连接成功' })
        }
      })
      this.currentClient.connect() // 连接蓝牙
    }

  }
```

- 控制连接状态
```typescript
  @State
  isConnect: boolean = false
```

- 在点击右侧蓝牙连接图标时调用
```typescript
Image($r('app.media.ic_sbbd_ly'))
                      .width(24)
                      .aspectRatio(1)
                      .onClick(() => {
                        blueManager.connectBlue(item.deviceId, () => {
                          this.isConnect = true
                        })
                      })
```

- 根据状态控制显示状态
```typescript
    if (!this.isConnect) {
            Row() {
              Text('未连接设备')
                .fontColor(ResManager.EC_MID_19)
                .fontSize(ResManager.EC_MODULE_TITLE_FS)
                .fontWeight(500)
              LoadingProgress()
                .margin({ left: 10 })
                .width(20)
                .height(20)
                .color(ResManager.EC_MID_99)
            }
            .width('100%')
            .margin({ top: 18, bottom: 18 })
          }
```

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719679650352-c6e7649f-da35-4e0f-9656-7147a6217e07.png#averageHue=%23f5f5f7&clientId=u1139f301-74b3-4&from=paste&height=777&id=u4571471a&originHeight=1554&originWidth=748&originalType=binary&ratio=1&rotation=0&showTitle=false&size=68470&status=done&style=none&taskId=uc1f92c03-3c01-4a14-acc5-3f3e24eb64d&title=&width=374)
> 提交代码


# 绑定设备接口调用
> 调用接口绑定当前的lot门禁设备

- 封装API接口
```typescript
/**
 * 绑定设备
 * @param iotId 物联网设备id
 * @returns
 */
export function bindDeviceApi(iotId: string): Promise<null> {
  return request({ url: `/employee/iot/bindingDevice/${iotId}`, method: 'put' })
}
```

- 在连接蓝牙成功后去跳转
```typescript
                    Image($r('app.media.ic_sbbd_ly'))
                      .width(24)
                      .aspectRatio(1)
                      .onClick(() => {
                        try {
                          blueManager.connectBlue(item.deviceId, async () => {
                            this.isConnect = true // 连接成功
                            // 因为蓝牙连接成功了 所以可以绑定了
                            await bindDeviceApi(currentLot.iotId!) // 物联网的id
                            // 进入下一步
                            router.pushUrl({
                              url: RouterConfig.DEVICE_BIND_TIP_URL, // 进行配置网络页面
                              params: {
                                tips: 1 // 成功的提示
                              }
                            })
                          }) // 连接蓝牙
                        } catch (error) {
                          router.pushUrl({
                            url: RouterConfig.DEVICE_BIND_TIP_URL, // 进行配置网络页面
                            params: {
                              tips: 2 // 失败的提示
                            }
                          })
                        }

                      })
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719685375913-94825b6d-f36f-48e9-8029-0761a27892b9.png#averageHue=%23fcfbfa&clientId=u1139f301-74b3-4&from=paste&height=774&id=u7519e11d&originHeight=1548&originWidth=722&originalType=binary&ratio=1&rotation=0&showTitle=false&size=81964&status=done&style=none&taskId=u54fbbdb8-7ceb-4b2a-aeac-b5390f20703&title=&width=361)
> 提交代码


# 获取我的设备
![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1714209995961-5d8b96fc-f616-4d40-a7c5-40195508bb0f.png#averageHue=%23a8d2ce&clientId=udc2b710d-f870-4&from=paste&height=532&id=Q9DKx&originHeight=1064&originWidth=564&originalType=binary&ratio=2&rotation=0&showTitle=false&size=107787&status=done&style=none&taskId=u87d2c9bb-3242-4d51-9f1e-af1d7ab580a&title=&width=282)

- 封装获取我的设备函数
```arkts
/**
 * 获取我的设备列表
 * @returns
 */
export function getMyDeviceListApi(): Promise<UnBindProductItemModel[]> {
  return request<UnBindProductItemModel[]>({ url: `/employee/iot/listDeviceByCurrentUser` })
}

/***
 * 解除绑定
 * **/
export function unbindDeviceApi(iotId: string): Promise<null> {
  return request({ url: `/employee/iot/unBindingDevice/${iotId}`, method: 'put' })
}
```

- 在我的设备中查询我的设备
```arkts
@State
  devices: UnBindProductItemModel[] = []

  async getMyDevices() {
    this.devices = await getMyDeviceListApi()
  }

  aboutToAppear(): void {
    this.getMyDevices()
  }
```

- 设备数据渲染
```arkts
        Scroll(this.scroller) {
          Column({ space: 20 }) {
            ForEach(this.devices, (prod: UnBindProductItemModel) => {
              Column() {
                // 标题
                Text(getDeviceMap(prod.productName)?.cn)
                  .fontColor(ResManager.EC_MID_19)
                  .fontSize(ResManager.EC_MODULE_TITLE_FS)
                  .fontWeight(500)
                  .margin({ bottom: 18 })
                // 内容
                GridRow({ columns: 2, gutter: { x: 10, y: 10 } }) {
                  ForEach(prod.deviceList, (item: DeviceItemModel, i: number) => {
                    GridCol() {
                      Stack({ alignContent: Alignment.TopEnd }) {
                        Column({ space: 10 }) {
                          Image(getDeviceMap(prod.productName)?.icon)
                            .width(80)
                            .aspectRatio(1)
                          Text(item.deviceName)
                            .fontColor(ResManager.EC_MID_19)
                            .fontSize(ResManager.EC_SMALL_FS)
                        }
                        .width('100%')
                        .padding({ top: 30, bottom: 20 })
                        .borderRadius(8)
                        .backgroundColor(ResManager.EC_MID_WHITE)
                        .onClick(() => {

                        })

                        Text('解绑')
                          .fontColor(ResManager.EC_MAIN_COLOR)
                          .fontSize(ResManager.EC_SMALL_FS)
                          .margin({ top: 10, right: 10 })
                          .onClick(() => {
                            this.unbind(item)
                          })

                      }
                    }
                    .onClick(() => {
                      router.back()
                    })
                  })
                }
              }
              .width('100%')
              .alignItems(HorizontalAlign.Start)
            })
          }
          .width('100%')
        }
        .scrollable(ScrollDirection.Vertical) // 滚动方向纵向
        .scrollBar(BarState.Off) // 滚动条常驻显示
```

- 设备解绑
```arkts
  // 解绑
  unbind(item: DeviceItemModel) {
    AlertDialog.show({
      title: '解除确认',
      message: '确认要解除该设备吗？',
      primaryButton: {
        value: '取消',
        fontColor: ResManager.EC_MID_59,
        action: () => {

        }
      },
      secondaryButton: {
        value: '确定',
        fontColor: ResManager.EC_MAIN_COLOR,
        action: async () => {
          await unbindDeviceApi(item.iotId)
          this.getMyDevices()
          promptAction.showToast({ message: '解绑成功' })
        }
      },
      alignment: DialogAlignment.Center
    })
  }
```
> 提交代码

# 设备配网-检查wifi状态
> 需求

![](https://cdn.nlark.com/yuque/0/2024/jpeg/380794/1714637523720-47ee8e1f-ffc0-46a9-86d5-66a98b94180c.jpeg)
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719685472307-dd89d8d3-a261-41f2-aa4d-782dfc682339.png#averageHue=%23949591&clientId=u1139f301-74b3-4&from=paste&height=599&id=u40f597f2&originHeight=1198&originWidth=666&originalType=binary&ratio=1&rotation=0&showTitle=false&size=141305&status=done&style=none&taskId=ucacc5a4a-1221-4dec-9fc4-9b5b17266f8&title=&width=333)

- 配置wifi网络权限
```typescript
{
        "name": "ohos.permission.GET_WIFI_INFO"
},
```

- 封装一个wifiManager
```typescript
class WfManager {
}

export const wfManager = new WfManager()
```

- 在utils/index.ets中导出
```typescript
export * from './WfMananger'
```

- 实现检查Wifi开关的方法
```typescript
import wifiManager from '@ohos.wifiManager';

class WfManager {
  checkWifi() {
    return wifiManager.isWifiActive()
  }
}

export const wfManager = new WfManager()
```

- 去配网跳转到配网页面
```typescript
EcDeviceTip({
          type: 'success',
          message: '绑定成功',
          btnText: '去配网',
          closeUrl: '',
          url: RouterConfig.DEVICE_DOOR_NET_URL
        })
```

- 配网页面检查勾选，确定时检查wifi
```typescript
@State
isOk: boolean = false
```

- 双向绑定指示灯闪灯
```typescript
  Checkbox()
                .select($$this.isOk)
                .selectedColor(ResManager.EC_MAIN_COLOR)
              Text('指示灯闪烁')
                .fontColor(ResManager.EC_MID_19)
                .fontSize(18)
                .fontWeight(500)
```

- 实现下一步逻辑
```typescript
 // 下一步逻辑
  next() {
    if (wfManager.checkWifi()) {
      router.pushUrl({
        url: RouterConfig.DEVICE_DOOR_NET_CONNECT_URL
      })
    } else {
      this.wifiDialog.open()
    }
  }
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719728816956-a5b64959-d103-4cad-a37e-67530c981cff.png#averageHue=%232d2d2d&clientId=u1139f301-74b3-4&from=paste&height=646&id=u6b3397f3&originHeight=646&originWidth=1266&originalType=binary&ratio=1&rotation=0&showTitle=false&size=63198&status=done&style=none&taskId=u9117fd3b-179f-46e1-b8ba-6861fe178a6&title=&width=1266)
> 提交代码


# 搜索wifi渲染列表
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719728882278-c0c7a666-aac6-4f44-b61c-7844ed9b83d4.png#averageHue=%2379cdc0&clientId=u1139f301-74b3-4&from=paste&height=569&id=u6dc78393&originHeight=569&originWidth=337&originalType=binary&ratio=1&rotation=0&showTitle=false&size=64750&status=done&style=none&taskId=ub2dd0932-3d10-4372-bb6d-8db68951ab5&title=&width=337)

- 点击wifi跳转到wifi扫描页
```typescript
 ListItem() {
              Row() {
                Text('YJY-5G')
                  .fontColor(ResManager.EC_MID_19)
                  .fontSize(ResManager.EC_MODULE_TITLE_FS)
                  .fontWeight(500)
                Row() {
                  Image($r('app.media.ic_arrow_black'))
                    .width(7)
                    .height(16)
                }
              }
              .width('100%')
              .padding({ top: 14, bottom: 14 })
              .justifyContent(FlexAlign.SpaceBetween)
              .onClick(() => {
                router.pushUrl({
                  url: RouterConfig.DEVICE_DOOR_SCAN_WIFI_URL
                })
              })
            }
```

- 在扫描页实现扫描wifi的方法
```typescript
@State
  list: wifiManager.WifiScanInfo[] = []

  aboutToAppear(): void {
    this.scanWifi()
  }

  scanWifi() {
    this.list = wifiManager.getScanInfoList()
  }
```

- 渲染wifi列表
```typescript
     // 设备列表
        List({ space: 10 }) {
          ForEach(
            this.list,
            (item: wifiManager.WifiScanInfo, i: number) => {
              ListItem() {
                Row() {
                  Column({ space: 5 }) {
                    Text(item.ssid)
                      .fontColor(ResManager.EC_MID_19)
                      .fontSize(ResManager.EC_NORMAL_FS)
                    Text(item.bssid)
                      .fontColor(ResManager.EC_MID_99)
                      .fontSize(ResManager.EC_SMALL_FS)
                  }
                  .alignItems(HorizontalAlign.Start)

                  Image(ResManager.IC_MJPW_WIFI)
                    .width(24)
                    .aspectRatio(1)
                }
                .width('100%')
                .justifyContent(FlexAlign.SpaceBetween)
                .padding({
                  left: 16,
                  right: 16,
                  top: 8,
                  bottom: 8
                })
                .borderRadius(ResManager.EC_NORMAL_RD)
                .backgroundColor('#fff')
              }
            })
        }
        .layoutWeight(1)
        .scrollBar(BarState.Off)
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719734509590-7a06ceda-02e5-4d69-bf9a-0192bf914584.png?x-oss-process=image/format,png#averageHue=%23fdfefa&clientId=u1139f301-74b3-4&from=paste&height=2720&id=u6d7e10c8&originHeight=2720&originWidth=1260&originalType=binary&ratio=1&rotation=0&showTitle=false&size=13709850&status=done&style=none&taskId=uc50ea3d1-fe98-408a-984f-2368b8cec04&title=&width=1260)
> 提交代码


# 选择wifi，绑定表单

- 点击某个wifi，返回wifi的ssid数据
```typescript
ListItem()
  ....
.onClick(() => {
                router.back({
                  url: RouterConfig.DEVICE_DOOR_NET_CONNECT_URL,
                  params: {
                    ssid: item.ssid
                  }
                })
              })
```

- 在表单页面双向绑定表单
```typescript

  @State
  wifiAccount: string = ''
  @State
  wifiPassword: string = ""
```

- 绑定表单

![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719734794558-fb074415-c458-428f-b0e4-8c9913566d21.png#averageHue=%232e2e2e&clientId=u1139f301-74b3-4&from=paste&height=898&id=ud1b154c4&originHeight=898&originWidth=1354&originalType=binary&ratio=1&rotation=0&showTitle=false&size=95787&status=done&style=none&taskId=ufaa7fb6b-057a-47a1-a227-633f7b723ba&title=&width=1354)
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719734804910-59a96bd8-bbf2-451a-b853-f55696eb2f29.png#averageHue=%23373535&clientId=u1139f301-74b3-4&from=paste&height=628&id=u11df19db&originHeight=628&originWidth=1808&originalType=binary&ratio=1&rotation=0&showTitle=false&size=109072&status=done&style=none&taskId=ue70e12fe-b85b-4689-bd77-525c3e3c83a&title=&width=1808)

- 在onPageShow中获取得到的ssid进行赋值
```typescript
  onPageShow(): void {
    const params = router.getParams() as object
    if (params && params["ssid"]) {
      this.wifiAccount = params["ssid"]
    }
  }
```
![image.png](https://cdn.nlark.com/yuque/0/2024/png/8435673/1719735149311-82cf23af-4e27-49b6-9643-4710dc519f75.png#averageHue=%2316c8b9&clientId=u1139f301-74b3-4&from=paste&height=1580&id=u91472b49&originHeight=1580&originWidth=794&originalType=binary&ratio=1&rotation=0&showTitle=false&size=149283&status=done&style=none&taskId=u95c8c50f-cf77-4866-82b1-0447610eea8&title=&width=794)
> 提交代码


# 发送wifi账号和密码给门禁
> 发送 WiFi 账号给设备：
> 1. 读写特征值：writeCharacteristicValue =》发送数据
> 2. 通知特征值：BLECharacteristicChange事件 =》接收数据

- 定义和门禁通信数据格式
```typescript
// 1. 蓝牙开门 2. 配置设备 wifi 连网
export interface BlueData {
  status?: 200 | 400 //  200 成功  400 失败
  msg?: string // 消息提示
  command?: 'open' | 'wifi'
  data?: string[] // 例如wifi=》 [ssid,pwd]
}
```
> 注意：该数据格式由硬件工程师和前端工程师共同商定

- 首先将之前的蓝牙连接改造成持久化状态
> 经过测试，蓝牙的实例对象创建之后，如果为局部变量，局部变量被回收之后，那么蓝牙也会断开

```typescript
currentClient?: ble.GattClientDevice

  // 连接蓝牙设备
  connectBlue(deviceId: string, callback: () => void) {
    if (deviceId) {
      this.currentClient?.disconnect()
      this.currentClient = ble.createGattClientDevice(deviceId) // 创建实例
      this.currentClient.on("BLEConnectionStateChange", (res) => {
        if (res.state === constant.ProfileConnectionState.STATE_CONNECTED) {
          callback()
          promptAction.showToast({ message: '蓝牙连接成功' })
        }
      })
      this.currentClient.connect()
    }
  }
```

- 点击下一步开始连接
```typescript
 Button('下一步')
          .margin({ top: 50 })
          .width(230)
          .backgroundColor(ResManager.EC_MAIN_COLOR)
          .enabled(!!(this.wifiAccount && this.wifiPassword))
          .onClick(() => {
            this.start()
          })
```

- 组装要发送的数据格式
```typescript
start() {
    this.linking = true
    this.run()
    try {
      // 数据
      const data: BlueData = {
        command: 'wifi',
        data: [this.wifiAccount, this.wifiPassword]
      }

    } catch (e) {
      Log.error(e)
    }
  }
```
> 说明：**重点了解蓝牙设备的连接和通信流程**
> **通信格式：JSON**
> 约定：**门禁设备服务serviceUuid以：0000AE30 开头**（16）
> 1. 发现门禁设备服务=》dev.getServices()
> 2. 根据约定查找服务=》serviceUuid
> 3. 获取读写特征值对象=》characteristics
>    1. 写入特征值以：**0000AE10 开头**
>    2. 通知特征值以：**0000AE04 开头**

- 封装发送数据方法
```typescript
// 给蓝牙发送数据
  async sendData(data: BlueData) {
    try {
      // 发送数据
      if (this.currentClient) {
        const list = await this.currentClient?.getServices()
        const doorService = list.find(item => item.serviceUuid.startsWith("0000AE30")) // 找到门禁服务
        if (doorService) {
          // 再找门禁服务的读写特征值的对象
          const writeObj =
            doorService.characteristics.find(item => item.characteristicUuid.startsWith("0000AE10")) // 写入特征值
          if (writeObj) {
            const encoder = new util.TextEncoder() // 生成一个编码对象
            const unit8Array = encoder.encodeInto(JSON.stringify(data))
            // 有了写入特征值 就可以通过蓝牙进行通信了
            await this.currentClient.writeCharacteristicValue({
              serviceUuid: writeObj.serviceUuid,
              characteristicUuid: writeObj.characteristicUuid,
              characteristicValue: unit8Array.buffer, // 将数据转化成二进制的arrayBuffer
              descriptors: []// 要写入的数据,
            }, ble.GattWriteType.WRITE)
          }
        }
      }
    } catch (error) {
      promptAction.showToast({ message: '发送数据失败，' + error.message })
    }

  }
```

- 在连接方法中监听wifi连接成功的命令
```typescript
  // 连接蓝牙设备
  connectBlue(deviceId: string, callback: () => void) {
    if (deviceId) {
      this.currentClient?.disconnect()
      this.currentClient = ble.createGattClientDevice(deviceId) // 创建实例
      this.currentClient.on("BLEConnectionStateChange", async (res) => {
        if (res.state === constant.ProfileConnectionState.STATE_CONNECTED) {
          callback()
          promptAction.showToast({ message: '蓝牙连接成功' })
          // 监听wifi成功连接
          this.currentClient?.on("BLECharacteristicChange", (res) => {
            // 接收蓝牙设备的数据响应
            const td = util.TextDecoder.create()
            const buff = new Uint8Array(res.characteristicValue)
            const result = JSON.parse(td.decodeWithStream(buff)) as BlueData
            if (result.command === 'wifi') {
              // 配网的响应处理
              emitter.emit("WifiConnect", {
                data: {
                  status: result.status
                }
              })
            }
          })
          const serviceList = await this.currentClient?.getServices() // 获取所有服务
          const ser = serviceList?.find(item => item.serviceUuid.startsWith('0000AE30'))
          const messageObj = ser?.characteristics.find(item => item.characteristicUuid.startsWith('0000AE04'))!
          await this.currentClient?.setCharacteristicChangeNotification(messageObj, true)
        }
      })

      this.currentClient.connect()
    }
  }
```

- 实现联网
```typescript

  // 开始连接网络
  async start() {
    this.linking = true
    this.run()
    try {
      // 数据
      const data: BlueData = {
        command: 'wifi',
        data: [this.wifiAccount, this.wifiPassword]
      }
      const isOk = await blueManager.sendData(data) // 发送数据
      promptAction.showToast({ message: isOk ? "发送账号成功" : "发送账号失败" })
    } catch (e) {
      Log.error(e)
    }
  }
  aboutToAppear(): void {
    emitter.on("WifiConnect", (res) => {
      router.pushUrl({
        url: RouterConfig.DEVICE_DOOR_CONNECT_TIP_URL,
        params: {
          tips: res.data?.status === 200 ? 1 : 2
        }
      })
    })
  }  
  aboutToDisappear(): void {
    clearInterval(this.timer)
  }
```
> 提交代码

# 设备控制

- 门禁管理

![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1714655037442-21c71823-a384-4957-8fa4-dcc94ba171ee.png#averageHue=%23595e5b&clientId=ua0e8f0b5-e054-4&from=paste&height=462&id=rttjI&originHeight=924&originWidth=592&originalType=binary&ratio=2&rotation=0&showTitle=false&size=317486&status=done&style=none&taskId=u62801821-0865-4d53-ac87-59068c680b8&title=&width=296)

- 点击我的设备进入
```typescript
.onClick(() => {
     currentLot.iotId = item.iotId
                          router.pushUrl({
                            url: RouterConfig.DEVICE_DOOR_CONTROL_URL,
                            params: {
                              iotId: item.iotId
                            }
                          })
                        })
```

- 封装门禁信息类型
```typescript
// 设备状态查询
export interface DeviceStatusData {
  /** 设备名称 */
  name: string;

  /** 设备状态，设备状态，ONLINE：设备在线，OFFLINE：设备离线，ABNORMAL：设备异常，INACTIVE：设备未激活，FROZEN：设备冻结 */
  status: string;
}
export class DeviceStatusDataModel implements DeviceStatusData {
  name: string = ''
  status: string = ''

  constructor(model: DeviceStatusData) {
    this.name = model.name
    this.status = model.status
  }
}

```

- 查询门禁信息-封装 api 函数
```arkts
/**
 * 获取设备状态
 * @param iotId 物联网设备id
 * @returns
 */
export function getDeviceStatusApi(iotId: string): Promise<DeviceStatusDataModel> {
  return request<DeviceStatusDataModel>({ url: `/employee/iot/getDeviceStatus/${iotId}` })
}
```

- 查询数据
```arkts
@State
  deviceStatus: DeviceStatusDataModel = new DeviceStatusDataModel({} as DeviceStatusData)

  aboutToAppear(): void {
    this.getDeviceStatus()
  }

  async getDeviceStatus() {
    const params = router.getParams() as object
    if (params && params["iotId"]) {
      this.deviceStatus = await getDeviceStatusApi(params["iotId"])
    }
  }
```

- 渲染数据
```arkts
Text(this.deviceStatus?.name)
  .fontColor(ResManager.EC_MID_19)
  .fontSize(ResManager.EC_MODULE_TITLE_FS)
  .margin({ top: 20 })
Text(this.deviceStatus.status === "ONLINE" ? "在线" : "离线")
  .fontColor('#fff')
  .fontSize(ResManager.EC_SMALL_FS)
  .padding({
    left: 14,
    right: 14,
    top: 2.5,
    bottom: 2.5
  })
```

- 替换跳转控制页面
```typescript
 list: ControlItem[] = [
    { name: '蓝牙开门', url: RouterConfig.DEVICE_DOOR_BLUE_OPEN_URL },
    { name: '远程开门', url: RouterConfig.DEVICE_DOOR_REMOTE_OPEN_URL },
    { name: '临时密码', url: RouterConfig.DEVICE_DOOR_PASSWORD_URL },
    { name: '开门记录', url: RouterConfig.DEVICE_DOOR_OPEN_RECORD_URL },
  ]
```

- 自动连接蓝牙
```typescript
// 是否连接到设备
@State
isLink: boolean = false
// 全屏 loading 弹层
  loading: CustomDialogController = new CustomDialogController({
    builder: EcLoading({ tip: '连接设备中...' }),
    customStyle: true,
    autoCancel: false,
    alignment: DialogAlignment.Center
  })
```

- 连接门禁
> 需求：从我的设备页面，点击门禁，进入门禁管理页面，进行设备鉴权和连接，为蓝牙开门准备
> 1. 请求蓝牙用户权限
> 2. 搜索门禁设备
> 3. 连接门禁设备，记录连接状态

```typescript
  aboutToAppear(): void {
    this.getDeviceStatus()
    this.connect()
  }

   // 连接蓝牙
  async connect() {
    this.loading.open()
    try {
      const isConnect = await blueManager.checkBlue() // 检查蓝牙
      if (isConnect) {
        blueManager.scanDevice((list) => {
          if (!this.isLink) {
            const blue = list.find(item => item.deviceName.startsWith("JL-ITHEIMA"))
            if (blue) {
              blueManager.connectBlue(blue.deviceId, () => {
                this.isLink = true
                this.loading.close()
              })
            }
          }
        })
      }
    } catch (error) {
      this.loading.close()
      promptAction.showToast({ message: '没找到设备' })
    }
  }
```
> 上课时的问题，由于连接过蓝牙之后，不能再次扫描到，必须要断开连接之后才可以连接，所以我们需要在返回页面上一个页面时先断掉蓝牙

- 声明一个断开蓝牙的方法
```typescript
  // 销毁蓝牙连接
  destroy() {
    if (this.currentClient) {
      this.currentClient.disconnect()
      this.currentClient.close()
      this.currentClient = null
    }
  }
```

- 在控制页面返回时进行断开
```typescript
onBackPress(): boolean | void {
    blueManager.destroy()
  }
```

> 提交代码

# 蓝牙开门
![image.png](https://cdn.nlark.com/yuque/0/2024/png/380794/1714725957628-0f645ff5-58a7-4809-b4b0-dfe5ee2f3669.png#averageHue=%23f5f9f9&clientId=u2741eb5b-0e4c-4&from=paste&height=387&id=u901e893e&originHeight=774&originWidth=600&originalType=binary&ratio=2&rotation=0&showTitle=false&size=52574&status=done&style=none&taskId=u390a3054-0759-4259-b9f9-b309a8df948&title=&width=300)

- 动画
> 分析：控制图片的左右晃动，播放声音

- 配置权限
> 传感器

```arkts
    "requestPermissions": [
      // ...
      {
        "name": "ohos.permission.ACCELEROMETER"
      }
    ]
```

1. 开启动画
```arkts
  // 摇一摇动画
  run() {
    this.timer = setInterval(() => {
      if (this.angle !== 30) {
        this.angle = 30
      } else {
        this.angle = 0
      }
      // 播放音频
      AVPlayerClass.playAudio("audio_shake.mp3")
    }, 200)
  }
```

- 关闭动画
```arkts
  stop() {
    setTimeout(() => {
    // 关闭动画
    clearInterval(this.timer)
    }, 1000)
  }
```

- 摇一摇检测
> 原理：当我们在晃动手机时，手机本身会产生一个加速度。我们可以**获取手机的两次加速度进行比较**，中间会有一个差值，当差值大于某个幅度时就可以认定用户进行了摇一摇这个操作。
> **通过传感器模块判定是否是摇一摇动作，执行业务**

**sensor.on**
on(type: SensorId.ACCELEROMETER, callback: Callback<AccelerometerResponse>, options?: Options): void
订阅加速度传感器数据。
**需要权限：** ohos.permission.ACCELEROMETER
**系统能力：** SystemCapability.Sensors.Sensor
**参数：**

| **参数名** | **类型** | **必填** | **说明** |
| --- | --- | --- | --- |
| type | [SensorId](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-sensor-0000001861887761-V5#ZH-CN_TOPIC_0000001881258313__sensorid9).ACCELEROMETER | 是 | 传感器类型，该值固定为SensorId.ACCELEROMETER。 |
| callback | Callback<[AccelerometerResponse](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-sensor-0000001861887761-V5#ZH-CN_TOPIC_0000001881258313__accelerometerresponse)> | 是 | 回调函数，异步上报的传感器数据固定为AccelerometerResponse。 |
| options | [Options](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-sensor-0000001861887761-V5#ZH-CN_TOPIC_0000001881258313__options) | 否 | 可选参数列表，用于设置传感器上报频率，默认值为200000000ns。 |

:::tips

1. **纳秒**（nanosecond，缩写为ns）是时间单位，表示的是一秒钟的十亿分之一。换算关系如下：

1 秒 = 1,000,000,000 纳秒

2. **m/s²**加速度的单位，表示速度在单位时间（秒）内的变化量。例如，如果一个物体原本以5 m/s的速度运动，然后在1秒内加速到10 m/s，那么它的加速度就是5 m/s²（因为速度增加了5 m/s）
:::
**错误码：**
以下错误码的详细介绍请参见 [ohos.sensor(传感器)错误码](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/errorcode-sensor-0000001815087942-V5)。

| 错误码ID | 错误信息 |
| --- | --- |
| 14500101 | Service exception. |

**示例：**
```arkts
import sensor from "@ohos.sensor";
import BusinessError from "@ohos.base";

try {
  sensor.on(sensor.SensorId.ACCELEROMETER, (data: sensor.AccelerometerResponse) => {
    console.info('Succeeded in invoking on. X-coordinate component: ' + data.x);
    console.info('Succeeded in invoking on. Y-coordinate component: ' + data.y);
    console.info('Succeeded in invoking on. Z-coordinate component: ' + data.z);
  }, { interval: 100000000 });
  setTimeout(() => {
    sensor.off(sensor.SensorId.ACCELEROMETER);
  }, 500);
} catch (error) {
  let e: BusinessError.BusinessError = error as BusinessError.BusinessError;
  console.error(`Failed to invoke on. Code: ${e.code}, message: ${e.message}`);
}
```

- 配置权限
```arkts
    "requestPermissions": [
      // ...
      {
        "name": "ohos.permission.ACCELEROMETER"
      }
    ]
```

- 实现检测
1. 监听动力加速度传感器
```arkts
  // 监听传感器
  onSensor() {
    sensor.on(sensor.SensorId.ACCELEROMETER, (data) => {
      let range = Math.abs(data.x - this.lastX) +
      Math.abs(data.y - this.lastY) +
      Math.abs(data.z - this.lastZ)

      // range是三个方向移动的范围 超过了 50就认为摇一摇了
      if (range > 50) {
        // 此时开始摇一摇 当它停止的时候 才进行逻辑
        this.isShake = true
      }
      if (this.isShake && range < 10) {
        // 此时认为 开始了摇一摇 并且停止了摇一摇
        // 可以动画 可以声音 可以开门了
        this.isShake = false // 再次等待下一摇一摇
        this.openDoor()
      }

      // x y z
      this.lastX = data.x
      this.lastY = data.y
      this.lastZ = data.z
    })
  }

```

2. 定义摇一摇检测状态

3. 定义摇一摇检测方法并调用
> 1. 获取手机两次加速度差值range
>    1. Math.abs(data.x - this.lastX) + Math.abs(data.y - this.lastY) + Math.abs(data.z - this.lastZ)
> 2. range 大于startRange阈值，开始摇动手机了
>    1. isShake：true
> 3. range 小于endRange同时上一次已经开始摇动手机了=> 摇一摇完成，执行业务
>    1. isShake：false
> 4. 记录最后一次摇一摇加速度

- 摇一摇逻辑
```arkts
  // 监听传感器
  onSensor() {
    sensor.on(sensor.SensorId.ACCELEROMETER, (data) => {
      let range = Math.abs(data.x - this.lastX) +
      Math.abs(data.y - this.lastY) +
      Math.abs(data.z - this.lastZ)

      // range是三个方向移动的范围 超过了 50就认为摇一摇了
      if (range > 50) {
        // 此时开始摇一摇 当它停止的时候 才进行逻辑
        this.isShake = true
      }
      if (this.isShake && range < 10) {
        // 此时认为 开始了摇一摇 并且停止了摇一摇
        // 可以动画 可以声音 可以开门了
        this.isShake = false // 再次等待下一摇一摇
        this.openDoor()
      }

      // x y z
      this.lastX = data.x
      this.lastY = data.y
      this.lastZ = data.z
    })
  }

```
说明：需要调整检测事件参数

- 摇一摇开门
```arkts
async openDoor() {
    this.run()
    await blueManager.sendData({
      command: "open"
    })
    this.stop()
    promptAction.showToast({ message: '开门成功' })
  }
```

> 提交代码

